services:
  db:
    image: postgres:15-alpine
    container_name: wallet_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: wallet_user
      POSTGRES_PASSWORD: wallet_password
      POSTGRES_DB: wallet_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U wallet_user -d wallet_db"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Dev compose intentionally db-only.
  # For the prod-shape API stack (api + migrate + caddy + tuned db),
  # use infra/docker-compose.prod.yml (added in plan 04-05).

volumes:
  postgres_data:

