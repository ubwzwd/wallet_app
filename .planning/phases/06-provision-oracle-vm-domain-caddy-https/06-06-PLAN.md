---
phase: 06-provision-oracle-vm-domain-caddy-https
plan: 06
type: execute
wave: 4
depends_on:
  - 06-05
files_modified:
  - infra/runbook-evidence/prod-verified.log
autonomous: false
requirements:
  - DOMAIN-02
  - DOMAIN-03
tags:
  - le-production
  - two-commit
  - hsts

must_haves:
  truths:
    - "infra/.env on VM has CADDY_ACME_CA= (empty) — Commit B of D-05 two-commit pattern applied"
    - "`docker compose restart caddy` ran successfully on the VM; caddy_data volume preserved (Pitfall 8 avoided)"
    - "Caddy logs after restart show issuance of a new certificate from Let's Encrypt production (issuer references Let's Encrypt R3/R10/E1 or similar prod issuer — NOT STAGING/Pretend Pear/Fake LE Root)"
    - "`curl -vI https://<your-domain>/` from operator laptop shows the LE prod issuer in the cert chain (no `-k` flag needed)"
    - "Regular (non-incognito) browser at `https://<your-domain>/` shows the wallet app with no cert warnings and a green padlock"
    - "HSTS header is present on responses: `curl -sI https://<your-domain>/` shows `strict-transport-security: max-age=31536000; includeSubDomains` (no preload token — per D-19)"
  artifacts:
    - path: "infra/runbook-evidence/prod-verified.log"
      provides: "Commit B timestamp marker confirming LE prod flip + HSTS verification"
      contains: "Commit B"
  key_links:
    - from: "infra/.env on VM CADDY_ACME_CA= (empty)"
      to: "Caddy default issuer chain (LE prod + ZeroSSL fallback)"
      via: "{$CADDY_ACME_CA} expansion in global block — empty -> Caddy default"
      pattern: "CADDY_ACME_CA="
    - from: "infra/Caddyfile HSTS directive"
      to: "https://<your-domain>/ response headers"
      via: "Caddy header directive applied on every response from the site block"
      pattern: "strict-transport-security"
---

<objective>
Operator-driven execution of RUNBOOK.md §11: the D-05 Commit B — flip CADDY_ACME_CA from staging to empty (LE prod), `docker compose restart caddy`, and verify the real LE prod cert is in place. Also closes DOMAIN-03 by observing HSTS header on prod-cert responses.

Purpose: This is the deliberate, separate second commit of the D-05 two-commit pattern. Once staging is verified (Plan 05 Task 3), this plan promotes the issuance to production. Plan separation prevents back-to-back execution of staging+prod that would defeat D-05's intent.

Output: 1 commit-B evidence log file committed to repo. VM is running with real LE prod cert. HSTS active.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-provision-oracle-vm-domain-caddy-https/06-CONTEXT.md
@.planning/phases/06-provision-oracle-vm-domain-caddy-https/06-RESEARCH.md
@infra/RUNBOOK.md
@infra/Caddyfile

<interfaces>
<!-- Plan 05 produced: VM with staging-cert in place; Commit A timestamp marker. -->
<!-- This plan: Commit B flip per D-05 two-commit hard requirement. -->

D-05 (CONTEXT.md): "Let's Encrypt staging endpoint first, then a separate commit flips to production (preserved from ROADMAP). [...] After staging-cert success is observed end-to-end, a separate, deliberate commit removes the override (or sets it to the production URL), and Caddy is restarted to fetch the prod cert. The two-commit pattern is a hard requirement, not a convention — the runbook calls this out explicitly."

D-19 (CONTEXT.md): "HSTS directive added to the Caddyfile site block in this phase [...] header Strict-Transport-Security \"max-age=31536000; includeSubDomains\" — exactly the ROADMAP-specified value, no preload [...] Becomes active automatically after the LE prod-cert flip in D-05."

RESEARCH Pattern 1 (Caddy `acme_ca` empty → default): "When `CADDY_ACME_CA` env var is unset OR empty string, Caddy's directive expansion produces `acme_ca` with no value → Caddy uses its default (LE prod + ZeroSSL)."

The single-line sed flip and `docker compose restart caddy` are RESEARCH Example 6 (lines 662-679).

LE prod cert issuers as of 2026: `R3`, `R10`, `R11`, `R12`, `E1`, `E2` (the active issuer chain rotates over time; staging chain uses `STAGING`, `Pretend Pear X1`, `Fake LE Root X2`). Operator's verification asserts the absence of staging chain markers + presence of `Let's Encrypt` in the issuer text.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Operator confirms Plan 05 Commit A is verified before proceeding (D-05 hard gate)</name>
  <files>
    Reads-only:
      infra/runbook-evidence/staging-verified.log
  </files>
  <read_first>
    - infra/runbook-evidence/staging-verified.log (from Plan 05 Task 3)
    - .planning/phases/06-provision-oracle-vm-domain-caddy-https/06-CONTEXT.md D-05
  </read_first>
  <what-built>
    Procedural gate (no automation). Operator confirms Commit A staging-cert verification was completed in Plan 05 Task 3 before applying Commit B (this plan).
  </what-built>
  <how-to-verify>
    Operator confirms (and pastes evidence in the resume message):
    1. `cat infra/runbook-evidence/staging-verified.log` shows at least one `Commit A (staging) verified at ...` line.
    2. Operator confirms they remember verifying the staging cert in an INCOGNITO browser per Plan 05 Task 3 step 8 (the cert subject matched `<your-domain>` and the issuer referenced STAGING / Pretend Pear / Fake LE Root).
    3. On VM: `grep '^CADDY_ACME_CA=' ~/wallet_app/infra/.env` still returns `CADDY_ACME_CA=https://acme-staging-v02.api.letsencrypt.org/directory` (i.e., we are at Commit A state, ready to flip to B).
  </how-to-verify>
  <resume-signal>
    Reply with one of:
    - `approved` (paste the 3 verification outputs)
    - `revert: <reason>` — if the operator wants to re-test Commit A before flipping. Revert means going back to Plan 05 Task 3.
  </resume-signal>
  <acceptance_criteria>
    - Commit A evidence file is present and confirmed
    - VM's .env is currently at staging-URL state
    - Operator explicitly attests staging cert was verified
  </acceptance_criteria>

  <action>
    Operator-driven checkpoint task. Follow the steps documented in <what-built> above and the referenced RUNBOOK sections in <read_first>. This is not an executor-agent task — the agent presents the checkpoint, the operator performs the steps on Oracle Cloud / Cloudflare / their laptop / the VM, then replies per <resume-signal>. The executor agent records the operator-provided evidence (pasted command outputs, file contents, descriptions) in the plan SUMMARY when the checkpoint is approved.
  </action>
  <verify>
    <human-check>Operator confirms (and pastes evidence in the resume message):
    1. `cat infra/runbook-evidence/staging-verified.log` shows at least one `Commit A (staging) verified at ...` line.
    2. Operator confirms they remember verifying the staging cert in an INCOGNITO browser per Plan 05 Task 3 step 8 (the cert subject matched `<your-domain>` and the issuer referenced STAGING / Pretend Pear / Fake LE Root).
    3. On VM: `grep '^CADDY_ACME_CA=' ~/wallet_app/infra/.env` still returns `CADDY_ACME_CA=https://acme-staging-v02.api.letsencrypt.org/directory` (i.e., we are at Commit A state, ready to flip to B).</human-check>
  </verify>
  <done>- Commit A evidence file is present and confirmed
    - VM's .env is currently at staging-URL state
    - Operator explicitly attests staging cert was verified</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator executes RUNBOOK §11 — Commit B LE prod flip + verify prod cert + verify HSTS header</name>
  <files>
    On VM (gitignored):
      ~/wallet_app/infra/.env (modified — CADDY_ACME_CA= empty)
    Committed to repo:
      infra/runbook-evidence/prod-verified.log
  </files>
  <read_first>
    - infra/RUNBOOK.md §11 (Flip to LE production — TWO-COMMIT pattern)
    - .planning/phases/06-provision-oracle-vm-domain-caddy-https/06-CONTEXT.md D-05, D-19
    - .planning/phases/06-provision-oracle-vm-domain-caddy-https/06-RESEARCH.md §"Pattern 1" (Caddy acme_ca empty → LE prod default), §"Pitfall 8" (no down -v)
  </read_first>
  <what-built>
    Operator-driven sequence on the VM as `deploy`:

    a. Single-line sed flip (no rewrite of the entire .env):
    ```bash
    cd ~/wallet_app/infra
    sed -i 's|^CADDY_ACME_CA=.*|CADDY_ACME_CA=|' .env
    grep '^CADDY_ACME_CA=' .env
    # Expected: CADDY_ACME_CA=  (empty value)
    ```

    b. Restart Caddy ONLY (not `down -v`, not `down`, not `up --force-recreate` — preserves caddy_data volume per Pitfall 8):
    ```bash
    docker compose -f docker-compose.prod.yml restart caddy
    ```

    c. Wait ~30s and verify Caddy re-issued the cert against LE prod:
    ```bash
    sleep 30
    docker compose -f docker-compose.prod.yml logs --tail 100 caddy 2>&1 | grep -iE "(obtained certificate|certificate obtained)" | tail -5
    # Expected: a new log line that does NOT reference STAGING/Pretend Pear/Fake LE
    ```

    d. From operator laptop (regular browser/curl, NOT incognito — the prod cert is trusted system-wide):
    ```bash
    curl -vI "https://<your-domain>/" 2>&1 | grep -iE 'issuer|subject'
    # Expected: issuer contains "Let's Encrypt" (typically R3, R10, R11, E1, etc.) — NOT STAGING/Pretend Pear/Fake LE.
    ```

    e. HSTS verification (D-19 / DOMAIN-03):
    ```bash
    curl -sI "https://<your-domain>/" | grep -i strict-transport-security
    # Expected: strict-transport-security: max-age=31536000; includeSubDomains
    # The header value must NOT include "preload" (D-19 explicit exclusion — Pitfall 7-related)
    ```

    f. Open `https://<your-domain>/` in REGULAR browser (Chrome/Firefox/Safari — not incognito). Expected: padlock icon present (no warning); wallet app loads.

    g. Conceptual Commit B timestamp marker:
    ```bash
    echo "Commit B (LE production) verified at $(date -u +%FT%TZ); HSTS observed" >> ~/wallet_app/infra/runbook-evidence/prod-verified.log
    ```

    h. Commit `infra/runbook-evidence/prod-verified.log` to repo (operator git add + commit on feature branch).
  </what-built>
  <how-to-verify>
    Operator confirms (and pastes evidence in the resume message):
    1. On VM: paste output of `grep '^CADDY_ACME_CA=' ~/wallet_app/infra/.env` — must show `CADDY_ACME_CA=` (empty value).
    2. On VM: paste output of `docker volume ls | grep caddy_data` — must list `wallet_app_caddy_data` or similar (volume still present — Pitfall 8 avoided).
    3. On VM: paste the last 5 lines of `docker compose -f ~/wallet_app/infra/docker-compose.prod.yml logs caddy 2>&1 | grep -iE "(obtained|certificate|issuance)"` — at least one line should NOT reference STAGING/Pretend Pear/Fake LE.
    4. From laptop: paste output of `curl -vI "https://<your-domain>/" 2>&1 | grep -iE 'issuer|subject' | head -5` — issuer must contain `Let's Encrypt` and must NOT contain STAGING/Pretend Pear/Fake LE.
    5. From laptop: paste output of `curl -sI "https://<your-domain>/" | grep -i strict-transport-security` — must show `strict-transport-security: max-age=31536000; includeSubDomains` (case-insensitive; case of "Strict-Transport-Security" header name may vary).
    6. From laptop: confirm `curl -sI "https://<your-domain>/" | grep -ci preload` returns 0 (HSTS preload not present — D-19 hard exclusion).
    7. Browser screenshot description (no upload needed): operator describes the padlock icon state — green padlock, no warning, in their regular non-incognito browser.
    8. Paste output of `cat infra/runbook-evidence/prod-verified.log` showing Commit B timestamp.
    9. Confirm `git ls-files infra/runbook-evidence/prod-verified.log` returns the path (file is committed).
  </how-to-verify>
  <resume-signal>
    Reply with one of:
    - `approved` (paste the 9 verification outputs as evidence; outputs #4 and #5 are the keystones — prod issuer + HSTS header)
    - `needs help: <describe the blocker>` — common blockers: cert still shows STAGING (caddy_data volume cached the staging cert state; the runbook restart should be sufficient; if not, check Caddy logs for any error; in rare cases `docker compose stop caddy && start caddy` may force a clean re-issuance; do NOT use `down -v` — that would destroy the ACME account state); HSTS header missing (verify Caddyfile change from Plan 01 actually deployed — `docker compose exec caddy cat /etc/caddy/Caddyfile | grep -i strict`); browser still warns about cert (Pitfall 7 — HSTS pin from staging-cert visit may persist in the operator's regular browser; clear via `chrome://net-internals/#hsts` for the domain)
  </resume-signal>
  <acceptance_criteria>
    - `.env` on VM has CADDY_ACME_CA= (empty); Commit B applied
    - caddy_data volume still present (Pitfall 8 avoided)
    - Caddy logs show new prod-cert issuance with non-STAGING issuer
    - curl from laptop confirms `Let's Encrypt` issuer (not STAGING)
    - HSTS header present with exact D-19 value; no `preload` token
    - Regular browser loads the app with green padlock
    - `infra/runbook-evidence/prod-verified.log` exists with Commit B timestamp, committed to repo
    - DOMAIN-02 satisfied (real LE prod cert)
    - DOMAIN-03 satisfied (HSTS header observed)
    - SC1 fully satisfied (https://<domain>/ loads with real LE cert)
  </acceptance_criteria>

  <action>
    Operator-driven checkpoint task. Follow the steps documented in <what-built> above and the referenced RUNBOOK sections in <read_first>. This is not an executor-agent task — the agent presents the checkpoint, the operator performs the steps on Oracle Cloud / Cloudflare / their laptop / the VM, then replies per <resume-signal>. The executor agent records the operator-provided evidence (pasted command outputs, file contents, descriptions) in the plan SUMMARY when the checkpoint is approved.
  </action>
  <verify>
    <human-check>Operator confirms (and pastes evidence in the resume message):
    1. On VM: paste output of `grep '^CADDY_ACME_CA=' ~/wallet_app/infra/.env` — must show `CADDY_ACME_CA=` (empty value).
    2. On VM: paste output of `docker volume ls | grep caddy_data` — must list `wallet_app_caddy_data` or similar (volume still present — Pitfall 8 avoided).
    3. On VM: paste the last 5 lines of `docker compose -f ~/wallet_app/infra/docker-compose.prod.yml logs caddy 2>&1 | grep -iE "(obtained|certificate|issuance)"` — at least one line should NOT reference STAGING/Pretend Pear/Fake LE.
    4. From laptop: paste output of `curl -vI "https://<your-domain>/" 2>&1 | grep -iE 'issuer|subject' | head -5` — issuer must contain `Let's Encrypt` and must NOT contain STAGING/Pretend Pear/Fake LE.
    5. From laptop: paste output of `curl -sI "https://<your-domain>/" | grep -i strict-transport-security` — must show `strict-transport-security: max-age=31536000; includeSubDomains` (case-insensitive; case of "Strict-Transport-Security" header name may vary).
    6. From laptop: confirm `curl -sI "https://<your-domain>/" | grep -ci preload` returns 0 (HSTS preload not present — D-19 hard exclusion).
    7. Browser screenshot description (no upload needed): operator describes the padlock icon state — green padlock, no warning, in their regular non-incognito browser.
    8. Paste output of `cat infra/runbook-evidence/prod-verified.log` showing Commit B timestamp.
    9. Confirm `git ls-files infra/runbook-evidence/prod-verified.log` returns the path (file is committed).</human-check>
  </verify>
  <done>- `.env` on VM has CADDY_ACME_CA= (empty); Commit B applied
    - caddy_data volume still present (Pitfall 8 avoided)
    - Caddy logs show new prod-cert issuance with non-STAGING issuer
    - curl from laptop confirms `Let's Encrypt` issuer (not STAGING)
    - HSTS header present with exact D-19 value; no `preload` token
    - Regular browser loads the app with green padlock
    - `infra/runbook-evidence/prod-verified.log` exists with Commit B timestamp, committed to repo
    - DOMAIN-02 satisfied (real LE prod cert)
    - DOMAIN-03 satisfied (HSTS header observed)
    - SC1 fully satisfied (https://<domain>/ loads with real LE cert)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator sed-edit of .env on VM | Single-line in-place edit; threat is wrong regex matching multiple lines (mitigated: sed pattern anchors to `^CADDY_ACME_CA=.*`) |
| `docker compose restart caddy` | Restart-only; threat is operator typing `down -v` muscle memory (Pitfall 8) |
| Caddy → Let's Encrypt production | Cert issuance against prod rate limit; threat is misconfiguration introduced between Commits A and B (mitigated: nothing else changes between A and B except the env var) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06-01 | Denial of Service | LE prod rate-limit (50 certs/week per registered domain) | mitigate | Plan 05 staging-first verified the configuration end-to-end. Plan 06 issues exactly 1 prod cert. The `caddy_data` named volume preserved across the restart means the ACME account is the same one used for staging — LE associates this with the same registration, avoiding "new account every restart" patterns. |
| T-06-06-02 | Denial of Service | caddy_data wipe via wrong compose command | mitigate | RUNBOOK §11 + Appendix C explicitly enumerate FORBIDDEN commands (down -v, volume rm). Task 2 verification step 2 asserts `docker volume ls | grep caddy_data` shows the volume present after restart. |
| T-06-06-03 | Tampering | HSTS header value drift | mitigate | Task 2 verification step 5 grep-matches the exact string `max-age=31536000; includeSubDomains`. Step 6 asserts `preload` is NOT present (D-19 hard exclusion). |
| T-06-06-04 | Spoofing | Wrong cert issuer (e.g., cert.local self-signed if Caddy fell back to internal) | mitigate | Task 2 verification step 4 explicitly requires issuer to contain `Let's Encrypt`. A `tls internal` fallback would show `Caddy Local Authority` — caught by this assertion. |
| T-06-06-05 | Tampering | Operator's regular browser pins staging cert HSTS, masks prod cert | accept | Pitfall 7 acknowledged — Plan 05 mitigated by requiring incognito-only staging verification. If the operator forgot incognito, the recovery path is `chrome://net-internals/#hsts` delete for the domain (documented). Plan 06 verification step 7 (browser padlock check) catches a successful prod cert in any working browser. |
| T-06-06-SC | Tampering | apt/package installs | n/a | No new packages introduced. Plan 06 is a config edit + container restart. |
</threat_model>

<verification>
- Task 1, 2 checkpoints approved by operator with pasted evidence
- `infra/runbook-evidence/prod-verified.log` exists and is committed
- VM is running with real LE prod cert + HSTS active
- DOMAIN-02 + DOMAIN-03 satisfied
- SC1 fully satisfied (the prod-cert portion was the missing piece after Plan 05)
- D-05 two-commit pattern observable in git history: Plan 05 commits include `staging-verified.log`; Plan 06 commits include `prod-verified.log` — separate commits per D-05 hard requirement
</verification>

<success_criteria>
- D-05 two-commit pattern honored end-to-end
- DOMAIN-02 (LE prod cert) + DOMAIN-03 (HSTS no preload) requirements satisfied
- SC1 fully satisfied (real LE prod cert in browser + DNS confirmed before first start)
- caddy_data volume preserved (Pitfall 8 avoided)
- 1 evidence artifact added: prod-verified.log
- Phase 6 is ~85% complete after this plan — only OPS-02 / SC5 monitoring + drill remain (Plan 07)
</success_criteria>

<output>
Create `.planning/phases/06-provision-oracle-vm-domain-caddy-https/06-06-SUMMARY.md` when done.
</output>
