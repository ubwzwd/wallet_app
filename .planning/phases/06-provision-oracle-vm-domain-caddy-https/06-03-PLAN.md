---
phase: 06-provision-oracle-vm-domain-caddy-https
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - SEC-02
tags:
  - roadmap
  - documentation
  - sec-02

must_haves:
  truths:
    - ".planning/ROADMAP.md Phase 6 Success Criterion 3 reads 'ufw allows only 23333/80/443' (not '22/80/443')"
    - "No other content in ROADMAP.md is modified — only the SC3 port-list literal text"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Amended Phase 6 SC3 port-list literal text per D-12"
      contains: "23333/80/443"
  key_links:
    - from: ".planning/ROADMAP.md Phase 6 SC3"
      to: "06-CONTEXT.md D-12 (SSH port noise reduction) + D-11 (Port 23333)"
      via: "literal text alignment between roadmap and locked phase decisions"
      pattern: "23333/80/443"
---

<objective>
Apply the trivial ROADMAP.md amendment locked in D-12: Phase 6 Success Criterion 3's literal port list `22/80/443` is updated to `23333/80/443` to reflect D-11's SSH-on-23333 decision. Intent is preserved (three ports, only deploy/web reachable); only the literal numbers change.

Purpose: ROADMAP.md is the source of truth that downstream verification (`/gsd:verify-work`, Plan 02's verifier `nmap` assertion) references. Mismatched literal text creates a forever-confusing source-vs-implementation drift.

Output: 1 file modified, 1 literal-text change. Trivial, single-task plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-provision-oracle-vm-domain-caddy-https/06-CONTEXT.md

<interfaces>
<!-- ROADMAP.md Phase 6 Success Criterion 3 — current literal text (line 79 of ROADMAP.md): -->
<!--    3. VM is hardened per SEC-02 baseline: `ufw` allows only 22/80/443; SSH is key-only ... -->
<!-- After this plan, the literal `22/80/443` becomes `23333/80/443`. -->
<!-- Other parts of SC3 (SSH key-only, PasswordAuthentication no, PermitRootLogin no, unattended-upgrades, non-root deploy user, nmap verification) remain unchanged in wording. -->

D-12 from 06-CONTEXT.md (lines 38-39):
> ROADMAP Phase 6 Success Criterion 3 is amended: literal text "ufw allows only 22/80/443" → "ufw allows only 23333/80/443" (intent preserved — three ports, only deploy/web reachable). This amendment is captured in CONTEXT.md and the planner's PLAN.md will reflect it; the literal ROADMAP text will be updated by the planner or executor as a trivial edit during this phase, not via a separate ROADMAP-edit ceremony.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend ROADMAP.md Phase 6 SC3 port list per D-12</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (focus lines 78-79 — Phase 6 Success Criteria block)
    - .planning/phases/06-provision-oracle-vm-domain-caddy-https/06-CONTEXT.md D-11 and D-12 (the lock that mandates this edit)
  </read_first>
  <action>
    Edit .planning/ROADMAP.md with a single literal-text change. In Phase 6 Success Criterion 3, the substring `22/80/443` is replaced with `23333/80/443`. The full SC3 line currently reads:

    `  3. VM is hardened per SEC-02 baseline: \`ufw\` allows only 22/80/443; SSH is key-only (\`PasswordAuthentication no\`, \`PermitRootLogin no\`); \`unattended-upgrades\` enabled for security patches; non-root deploy user with sudo; \`nmap\` from outside shows only the three allowed ports.`

    After the edit, it reads:

    `  3. VM is hardened per SEC-02 baseline: \`ufw\` allows only 23333/80/443; SSH is key-only (\`PasswordAuthentication no\`, \`PermitRootLogin no\`); \`unattended-upgrades\` enabled for security patches; non-root deploy user with sudo; \`nmap\` from outside shows only the three allowed ports.`

    Use the Edit tool (not Bash sed) to make the change. The old_string must include enough surrounding context to uniquely identify Phase 6 SC3 (not Phase 4 or Phase 7, which may have unrelated port references). Recommended unique context:
    - old_string: `VM is hardened per SEC-02 baseline: \`ufw\` allows only 22/80/443; SSH is key-only`
    - new_string: `VM is hardened per SEC-02 baseline: \`ufw\` allows only 23333/80/443; SSH is key-only`

    Do NOT modify any other line in ROADMAP.md. Do NOT change the SC3 ordering, the SC numbering (1-5), the Phase 6 Goal line, the Plans count, or any other phase's content.

    No SECOND amendment is needed elsewhere in ROADMAP.md — `22/80/443` does not appear in Phase 4 or Phase 7 success criteria, only in Phase 6 SC3. Confirm with a pre-edit grep: `grep -n '22/80/443' .planning/ROADMAP.md` should return exactly one line (Phase 6 SC3) before the edit, and zero lines after.
  </action>
  <verify>
    <automated>grep -c '22/80/443' .planning/ROADMAP.md  # must return 0 after the edit</automated>
    <automated>grep -c '23333/80/443' .planning/ROADMAP.md  # must return 1</automated>
    <automated>grep -c '^### Phase ' .planning/ROADMAP.md  # must still return 4 (Phase 4, 5, 6, 7) — no structural change</automated>
    <automated>grep -c '^  3\. ' .planning/ROADMAP.md  # must still return 4 (one SC3 per phase) — no SC reordering</automated>
    <automated>grep -A1 'ufw\` allows only 23333/80/443' .planning/ROADMAP.md | grep -q 'PasswordAuthentication no'  # full SC3 line intact except for the port substitution</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` contains exactly one occurrence of `23333/80/443` and zero occurrences of `22/80/443`
    - Phase 6 Success Criterion 3 otherwise unchanged: still references `ufw`, `PasswordAuthentication no`, `PermitRootLogin no`, `unattended-upgrades`, non-root deploy user, `nmap` from outside
    - All four phase headers (Phase 4, 5, 6, 7) still present
    - Total ROADMAP.md line count changes by zero (single literal-text substitution, no line additions/deletions)
    - No other phase's success criteria are modified
  </acceptance_criteria>
  <done>ROADMAP.md Phase 6 SC3 port list reflects D-11 + D-12 (SSH on 23333). No other roadmap content modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning docs ↔ implementation | ROADMAP.md is the source of truth that verifier scripts and `/gsd:verify-work` reference. Threat is drift between roadmap text and locked CONTEXT decisions, causing verification to look at the wrong port. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Tampering | ROADMAP.md SC3 literal text | mitigate | Verify-task automated checks include both positive (`grep 23333/80/443` returns 1) and negative (`grep 22/80/443` returns 0) assertions. Edit tool's old_string/new_string requirement prevents accidental multi-match. |
| T-06-03-02 | Repudiation | Phase 6 SC3 verification cross-reference | mitigate | After this plan, `nmap` assertion in verify-phase6.sh (Plan 02) and the operator's RUNBOOK §13 (Plan 04) both reference 23333/80/443 — consistent with the amended roadmap. |
| T-06-03-SC | Tampering | Package installs | n/a | No packages introduced in this plan. |
</threat_model>

<verification>
- `grep -c '22/80/443' .planning/ROADMAP.md` returns 0
- `grep -c '23333/80/443' .planning/ROADMAP.md` returns 1
- ROADMAP.md structure (headers, SC numbering, phase ordering) unchanged
</verification>

<success_criteria>
- ROADMAP.md Phase 6 SC3 port list reads `23333/80/443`
- D-12 locked decision honored
- No other roadmap content drift
</success_criteria>

<output>
Create `.planning/phases/06-provision-oracle-vm-domain-caddy-https/06-03-SUMMARY.md` when done.
</output>
