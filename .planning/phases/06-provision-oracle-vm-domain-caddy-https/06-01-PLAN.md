---
phase: 06-provision-oracle-vm-domain-caddy-https
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/Caddyfile
  - infra/.env.example
  - infra/docker-compose.prod.yml
autonomous: true
requirements:
  - DOMAIN-02
  - DOMAIN-03
  - DOMAIN-04
tags:
  - caddy
  - hsts
  - acme
  - letsencrypt
  - env-vars

must_haves:
  truths:
    - "Caddyfile contains a global block with `acme_ca {$CADDY_ACME_CA}` directive above the site block"
    - "Caddyfile site block contains `header Strict-Transport-Security \"max-age=31536000; includeSubDomains\"` (no preload token)"
    - "infra/.env.example documents CADDY_ACME_CA with staging URL as authored default and explains the empty-string=LE-prod flip"
    - "infra/.env.example narrows ALLOWED_ORIGINS from https://localhost to a CHANGE_ME_YOUR_DOMAIN sentinel"
    - "docker-compose.prod.yml caddy service exports CADDY_ACME_CA via the environment: block so Caddy sees the var at container start"
    - "`docker run --rm -v $PWD/infra/Caddyfile:/etc/caddy/Caddyfile:ro caddy:alpine caddy validate --config /etc/caddy/Caddyfile --adapter caddyfile` exits 0"
  artifacts:
    - path: "infra/Caddyfile"
      provides: "Final Caddyfile with global acme_ca + HSTS header"
      contains: "acme_ca {$CADDY_ACME_CA}"
    - path: "infra/Caddyfile"
      provides: "HSTS header directive inside the site block"
      contains: "Strict-Transport-Security"
    - path: "infra/.env.example"
      provides: "Documented CADDY_ACME_CA variable with staging default"
      contains: "CADDY_ACME_CA"
    - path: "infra/.env.example"
      provides: "Narrowed ALLOWED_ORIGINS sentinel"
      contains: "CHANGE_ME_YOUR_DOMAIN"
    - path: "infra/docker-compose.prod.yml"
      provides: "Caddy service env passthrough for CADDY_ACME_CA"
      contains: "CADDY_ACME_CA"
  key_links:
    - from: "infra/.env (on VM, derived from .env.example)"
      to: "infra/docker-compose.prod.yml caddy service environment block"
      via: "compose interpolation: CADDY_ACME_CA: ${CADDY_ACME_CA:-}"
      pattern: "CADDY_ACME_CA:\\s*\\$\\{CADDY_ACME_CA"
    - from: "infra/docker-compose.prod.yml caddy environment block"
      to: "infra/Caddyfile global acme_ca directive"
      via: "Caddy {$CADDY_ACME_CA} env-var expansion at container start"
      pattern: "acme_ca\\s*\\{\\$CADDY_ACME_CA\\}"
---

<objective>
Add HTTPS production-readiness to the Phase 4 prod stack by extending `infra/Caddyfile` with the global `acme_ca` directive (env-var-driven, per D-05) and the HSTS header (D-19, deferred from Phase 4 D-11), and extending `infra/.env.example` + `infra/docker-compose.prod.yml` to thread the new `CADDY_ACME_CA` env var through the compose pipeline.

Purpose: Establishes the **two-commit pattern wiring** (D-05) for the Let's Encrypt staging→prod flip. After this plan: a single `.env` line edit on the VM (Commit B) swaps from LE staging to LE prod with `docker compose restart caddy` — no Caddyfile diff in the flip commit.

Output: 3 files modified (Caddyfile, .env.example, docker-compose.prod.yml). Zero new files. Caddy `caddy validate` exits 0 against the new Caddyfile.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-provision-oracle-vm-domain-caddy-https/06-CONTEXT.md
@.planning/phases/06-provision-oracle-vm-domain-caddy-https/06-RESEARCH.md
@.planning/phases/06-provision-oracle-vm-domain-caddy-https/06-PATTERNS.md
@infra/Caddyfile
@infra/.env.example
@infra/docker-compose.prod.yml

<interfaces>
<!-- Existing Caddyfile structure (27 lines) — additive edits only, no reordering -->

Current `infra/Caddyfile`:
- Line 1: `{$CADDY_DOMAIN:localhost} {`  (site block opener — KEEP unchanged)
- Line 2: `    tls {$CADDY_TLS_MODE:internal}`  (KEEP unchanged)
- Lines 6-20: `handle /api/*`, `handle /health`, `handle /docs*`, `handle /openapi.json`  (KEEP unchanged — do NOT reorder)
- Lines 22-26: `handle { root * /srv; try_files {path} /index.html; file_server }` (KEEP unchanged)

Phase 4 D-10 env-var interpolation idioms:
- `{$VAR:default}` — falls back to literal `default` if env var unset/empty
- `{$VAR}` — expands to empty string if env var unset/empty  (this form is what `acme_ca {$CADDY_ACME_CA}` exploits per RESEARCH Pattern 1)

Current `infra/.env.example` schema (37 lines): documented in 06-PATTERNS.md §"infra/.env.example".
Notable existing keys to preserve: POSTGRES_USER/PASSWORD/DB, DATABASE_URL, SECRET_KEY, ACCESS_TOKEN_EXPIRE_MINUTES, ALGORITHM, ALLOWED_ORIGINS, EXCHANGE_RATE_API_URL, ENVIRONMENT, DEBUG, CADDY_DOMAIN, CADDY_TLS_MODE.

Current `infra/docker-compose.prod.yml` caddy service (lines 61-78):
- `env_file: ./.env`  (NOT present on caddy service — it uses `environment:` block only; this is intentional Phase 4 design)
- `environment:` block exports `CADDY_DOMAIN: ${CADDY_DOMAIN:-localhost}` and `CADDY_TLS_MODE: ${CADDY_TLS_MODE:-internal}` (lines 65-67)
- Phase 6 adds one more line to that block: `CADDY_ACME_CA: ${CADDY_ACME_CA:-}` (defaults to empty)

Validation command (RESEARCH §Validation Strategy / 06-VALIDATION.md Wave 0):
`docker run --rm -v "$(pwd)/infra/Caddyfile:/etc/caddy/Caddyfile:ro" caddy:alpine caddy validate --config /etc/caddy/Caddyfile --adapter caddyfile`
Exit 0 = Caddyfile syntactically valid.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add acme_ca global block + HSTS header to infra/Caddyfile</name>
  <files>infra/Caddyfile</files>
  <read_first>
    - infra/Caddyfile (current 27-line file — additive edits only)
    - .planning/phases/06-provision-oracle-vm-domain-caddy-https/06-RESEARCH.md §"Pattern 1: Caddyfile Global acme_ca" and §"Code Examples" Example 1 (lines 530-567)
    - .planning/phases/06-provision-oracle-vm-domain-caddy-https/06-CONTEXT.md D-05 (acme_ca staging→prod) and D-19 (HSTS exact value, no preload)
    - .planning/phases/06-provision-oracle-vm-domain-caddy-https/06-PATTERNS.md §"infra/Caddyfile" §"Phase 6 deltas"
  </read_first>
  <action>
    Edit infra/Caddyfile additively (no reordering, no deletions). Two changes:

    Change 1 — Prepend a Caddy global options block above the existing site block. The block opens with a bare `{` on its own line, contains one directive `acme_ca {$CADDY_ACME_CA}` indented 4 spaces, and closes with `}` on its own line. Leave one blank line between the closing `}` of the global block and the existing line 1 (`{$CADDY_DOMAIN:localhost} {`).

    Per D-05 and RESEARCH Pattern 1: an empty/unset CADDY_ACME_CA causes Caddy to use its default issuer chain (LE prod + ZeroSSL fallback), so the two-commit LE staging→prod flip is just an .env line edit on the VM — no Caddyfile change in Commit B.

    Change 2 — Inside the existing site block, between the `tls {$CADDY_TLS_MODE:internal}` line and the `encode zstd gzip` line, insert one blank line then add the HSTS header directive:
    `    header Strict-Transport-Security "max-age=31536000; includeSubDomains"`
    Indentation: exactly 4 spaces (matching every other directive in the site block). Value: exact string per D-19, no `preload` token (Pitfall 7 — irreversible). Quoting: double quotes around the full header value.

    Do NOT modify, reorder, or delete any of the existing `handle` blocks, the `encode` directive, the `tls` directive, the `{$CADDY_DOMAIN:localhost}` placeholder, or any closing brace. The structural delta is exactly: +6 lines (1 blank + 4-line global block + 1 blank), +2 lines inside site block (1 blank + HSTS directive). Final file length is approximately 35 lines.

    Per D-19 and RESEARCH Pitfall 7: HSTS becomes active on the first browser visit after the LE prod cert is in place. Operator MUST verify the staging cert in an incognito/private browser window during the staging step (Plan 05a) so the HSTS pin from the staging-cert (untrusted "Fake LE Root") does not brick the operator's regular browser session post-prod-flip. This is a runbook callout (Plan 04), not a Caddyfile change.
  </action>
  <verify>
    <automated>docker run --rm -v "$(pwd)/infra/Caddyfile:/etc/caddy/Caddyfile:ro" caddy:alpine caddy validate --config /etc/caddy/Caddyfile --adapter caddyfile</automated>
    <automated>grep -c 'acme_ca {$CADDY_ACME_CA}' infra/Caddyfile  # must return 1</automated>
    <automated>grep -c 'Strict-Transport-Security "max-age=31536000; includeSubDomains"' infra/Caddyfile  # must return 1</automated>
    <automated>grep -c 'preload' infra/Caddyfile  # must return 0 — D-19 forbids preload</automated>
    <automated>grep -c '^    handle' infra/Caddyfile  # must return 5 (4 named handles + 1 default) — verifies no handles deleted</automated>
  </verify>
  <acceptance_criteria>
    - Caddyfile contains exactly one global block opening (a bare `{` on its own line before the site block)
    - Caddyfile contains exactly one `acme_ca {$CADDY_ACME_CA}` directive, inside the global block
    - Caddyfile contains exactly one HSTS `header` directive with value `Strict-Transport-Security "max-age=31536000; includeSubDomains"` (no `preload` token anywhere in file)
    - All 5 existing `handle` blocks (handle /api/*, handle /health, handle /docs*, handle /openapi.json, handle{} default) remain unchanged in name and order
    - `caddy validate` exits 0 against the modified file
    - `tls {$CADDY_TLS_MODE:internal}` directive remains untouched (still falls back to `internal` on laptop where CADDY_TLS_MODE is set; on VM it will be unset → Caddy uses ACME)
  </acceptance_criteria>
  <done>Caddyfile structurally extended with global acme_ca block and HSTS directive; `caddy validate` green; no reordering or deletion of existing directives.</done>
</task>

<task type="auto">
  <name>Task 2: Extend infra/.env.example with CADDY_ACME_CA + narrow ALLOWED_ORIGINS</name>
  <files>infra/.env.example</files>
  <read_first>
    - infra/.env.example (current 37-line file)
    - .planning/phases/06-provision-oracle-vm-domain-caddy-https/06-RESEARCH.md §"Code Examples" Example 2 (lines 569-604)
    - .planning/phases/06-provision-oracle-vm-domain-caddy-https/06-CONTEXT.md D-05 (CADDY_ACME_CA), D-20 (ALLOWED_ORIGINS narrowing)
    - .planning/phases/06-provision-oracle-vm-domain-caddy-https/06-PATTERNS.md §"infra/.env.example" §"Phase 6 deltas"
  </read_first>
  <action>
    Edit infra/.env.example with three changes. Preserve the existing top-of-file USAGE comment block (lines 1-9) and all existing variables verbatim except where called out.

    Change 1 — Narrow ALLOWED_ORIGINS sentinel (per D-20). Replace the single line `ALLOWED_ORIGINS=https://localhost` (currently at line 25) with `ALLOWED_ORIGINS=https://CHANGE_ME_YOUR_DOMAIN`. Update the preceding comment (line 24) to: `# CORS — narrow from https://localhost to real prod domain on the VM (DOMAIN-04, defense-in-depth)`. The `CHANGE_ME_YOUR_DOMAIN` sentinel matches the existing CHANGE_ME_* idiom (lines 12-14, 20) so a forgotten substitution fails loudly. **NOTE — Phase 4 smoke compatibility:** the Phase 4 `infra/scripts/smoke.sh` line 41 hard-codes the list of keys it grep-tests; `ALLOWED_ORIGINS` is in that list, so the literal value change is fine (the test only checks key presence, not value). No smoke.sh edit required.

    Change 2 — Document CADDY_DOMAIN/CADDY_TLS_MODE swap rationale. Keep the existing two lines `CADDY_DOMAIN=localhost` and `CADDY_TLS_MODE=internal` (lines 35-36) verbatim — they preserve laptop bring-up behavior. Replace the existing line-34 comment with a 4-line multi-line comment block immediately above the two CADDY_DOMAIN/CADDY_TLS_MODE lines:
    `# Caddy (laptop default: tls internal self-signed at https://localhost; VM swaps CADDY_DOMAIN to real domain and COMMENTS OUT CADDY_TLS_MODE so Caddy uses ACME)`
    `# VM .env: set CADDY_DOMAIN=<your-domain>, then comment out or remove the CADDY_TLS_MODE line entirely (do not set it to ""; comment removal causes Caddy to fall back to ACME)`

    Change 3 — Append a new section at the end of the file (after line 36) introducing CADDY_ACME_CA. Insert one blank line then add a 5-line block:
    `# Caddy ACME endpoint (Phase 6 NEW, D-05 staging→prod two-commit pattern).`
    `#   - On laptop: leave UNSET — Caddy uses tls internal (no ACME).`
    `#   - On VM, first bring-up (Commit A): set to staging URL below — DO NOT REMOVE until staging cert is verified.`
    `#   - On VM, after staging cert verified (Commit B): set empty (CADDY_ACME_CA=) so Caddy defaults to LE prod.`
    `# Staging URL: https://acme-staging-v02.api.letsencrypt.org/directory`
    `CADDY_ACME_CA=`

    The value is set to empty (laptop default — Caddy falls back to no ACME because CADDY_DOMAIN=localhost triggers tls internal). The staging URL is in the comment, not assigned, so a `cp .env.example .env` on the laptop continues to work in tls internal mode. Operators on the VM override the value when authoring the VM's `.env` (this is documented in the RUNBOOK in Plan 04).

    Final file length: approximately 50 lines (37 existing + 13 new).
  </action>
  <verify>
    <automated>grep -c '^ALLOWED_ORIGINS=https://CHANGE_ME_YOUR_DOMAIN$' infra/.env.example  # must return 1</automated>
    <automated>grep -c '^ALLOWED_ORIGINS=https://localhost$' infra/.env.example  # must return 0 — old value removed</automated>
    <automated>grep -c '^CADDY_ACME_CA=' infra/.env.example  # must return 1</automated>
    <automated>grep -c 'acme-staging-v02.api.letsencrypt.org' infra/.env.example  # must return 1 (in comment)</automated>
    <automated>grep -cE '^(POSTGRES_USER|POSTGRES_PASSWORD|POSTGRES_DB|SECRET_KEY|ALLOWED_ORIGINS|CADDY_DOMAIN|CADDY_TLS_MODE|CADDY_ACME_CA)=' infra/.env.example  # must return 8 — Phase 4 smoke.sh grep list still satisfied plus new key</automated>
    <automated>bash infra/scripts/env-coverage.sh  # Phase 4 coverage check must still pass — CADDY_ACME_CA is not a FastAPI Settings field, so it does not appear in the coverage assertion</automated>
  </verify>
  <acceptance_criteria>
    - ALLOWED_ORIGINS value is exactly `https://CHANGE_ME_YOUR_DOMAIN` (sentinel form matching D-20)
    - CADDY_DOMAIN=localhost and CADDY_TLS_MODE=internal remain present as laptop defaults (Phase 4 smoke compatibility per smoke.sh line 41 grep list)
    - CADDY_ACME_CA is documented with 5+ lines of inline comment explaining staging/prod/laptop usage, with the staging URL in the comment and the value set to empty
    - All 7 existing CHANGE_ME_* sentinels (POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_DB, SECRET_KEY, ALLOWED_ORIGINS, plus new CHANGE_ME_YOUR_DOMAIN inside ALLOWED_ORIGINS) remain
    - env-coverage.sh from Phase 4 still exits 0
    - Phase 4's smoke.sh grep list (line 41: POSTGRES_USER POSTGRES_PASSWORD POSTGRES_DB SECRET_KEY ALLOWED_ORIGINS CADDY_DOMAIN CADDY_TLS_MODE) still finds every key
  </acceptance_criteria>
  <done>.env.example documents CADDY_ACME_CA, narrows ALLOWED_ORIGINS to a CHANGE_ME sentinel, preserves laptop defaults, and remains compatible with Phase 4 verifiers.</done>
</task>

<task type="auto">
  <name>Task 3: Add CADDY_ACME_CA env passthrough to caddy service in docker-compose.prod.yml</name>
  <files>infra/docker-compose.prod.yml</files>
  <read_first>
    - infra/docker-compose.prod.yml (current 84-line file — focus lines 61-78 caddy service)
    - .planning/phases/06-provision-oracle-vm-domain-caddy-https/06-PATTERNS.md §"Shared Patterns" §"Pattern A: Env-var-driven config" (lines 317-341)
    - .planning/phases/06-provision-oracle-vm-domain-caddy-https/06-CONTEXT.md D-05 (CADDY_ACME_CA threading)
  </read_first>
  <action>
    Edit infra/docker-compose.prod.yml. Single change to the caddy service `environment:` block (currently lines 65-67):

    Existing block (two lines):
    `      CADDY_DOMAIN: ${CADDY_DOMAIN:-localhost}`
    `      CADDY_TLS_MODE: ${CADDY_TLS_MODE:-internal}`

    Add one line below them, preserving 6-space indentation (matching siblings):
    `      CADDY_ACME_CA: ${CADDY_ACME_CA:-}`

    The `:-` empty-string default ensures that when CADDY_ACME_CA is unset (laptop) OR set to empty (VM Commit B post-prod-flip), compose substitutes the empty string, which Caddy's `{$CADDY_ACME_CA}` directive expansion in the Caddyfile then expands to a bare `acme_ca` with no argument → Caddy default issuer chain (LE prod + ZeroSSL fallback) per RESEARCH Pattern 1.

    Do NOT add `env_file: ./.env` to the caddy service — Phase 4 D-10 chose the explicit `environment:` block specifically so secrets (DATABASE_URL, SECRET_KEY, POSTGRES_PASSWORD) are NOT injected into the caddy container. Only the three CADDY_* keys belong here.

    Do NOT modify any other service (db, migrate, api) — they continue using `env_file: ./.env`. Do NOT modify the volumes block (caddy_data, caddy_config, wallet_pgdata_prod must stay — losing caddy_data would burn LE issuance attempts per Pitfall 8).
  </action>
  <verify>
    <automated>docker compose -f infra/docker-compose.prod.yml config -q  # compose lint exits 0</automated>
    <automated>grep -c 'CADDY_ACME_CA: \${CADDY_ACME_CA:-}' infra/docker-compose.prod.yml  # must return 1</automated>
    <automated>grep -c '^volumes:' infra/docker-compose.prod.yml  # must return 1 — volumes block preserved</automated>
    <automated>grep -cE '^  (db|migrate|api|caddy):' infra/docker-compose.prod.yml  # must return 4 — all four services intact</automated>
    <automated>grep -c 'caddy_data:/data' infra/docker-compose.prod.yml  # must return 1 — Caddy data volume mount preserved (Pitfall 8 guard)</automated>
  </verify>
  <acceptance_criteria>
    - caddy service `environment:` block contains exactly 3 keys: CADDY_DOMAIN, CADDY_TLS_MODE, CADDY_ACME_CA — in that order
    - CADDY_ACME_CA uses `${CADDY_ACME_CA:-}` substitution form (empty default, not missing default)
    - No `env_file:` directive on the caddy service (Phase 4 D-10 isolation preserved)
    - All four services (db, migrate, api, caddy) present with their original config
    - All three volumes (wallet_pgdata_prod, caddy_data, caddy_config) declared
    - `docker compose config` exits 0 with both `CADDY_ACME_CA=` set and unset in current shell env
  </acceptance_criteria>
  <done>docker-compose.prod.yml threads CADDY_ACME_CA from VM .env through to the caddy container's env; compose config lints clean; all other services and volumes untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator laptop → Caddyfile (in repo) | Plan-time edits; threat is human-induced misconfig (HSTS too long without prod cert; acme_ca pointed at wrong endpoint) |
| Caddyfile env-var interpolation | Caddy expands `{$CADDY_ACME_CA}` at container start; threat is unset/wrong env var → wrong ACME endpoint → LE rate-limit burn |
| .env.example (committed) → operator-authored .env (gitignored) | Schema-to-actual-secrets boundary; threat is copy-paste of staging URL into a Commit B that should empty it |
| docker-compose.prod.yml → caddy container | Compose interpolation `${CADDY_ACME_CA:-}` at `docker compose up`; threat is shell-env contamination from the operator's parent shell |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Tampering | Caddyfile HSTS directive | mitigate | D-19 hard-codes the exact header value (`max-age=31536000; includeSubDomains` — no `preload`). Verify task includes `grep -c preload infra/Caddyfile` must return 0. Pitfall 7 (HSTS-bricks-staging-cert-browsers) is handled in Plan 04 runbook (incognito-only staging verification). |
| T-06-01-02 | Information Disclosure | docker-compose.prod.yml caddy service env | accept | The `environment:` block exports only CADDY_DOMAIN/CADDY_TLS_MODE/CADDY_ACME_CA — all non-secrets (domain, public ACME URL). Plan 04 enforces `chmod 600 infra/.env` on the VM so the secret-bearing .env is not world-readable. |
| T-06-01-03 | Denial of Service | LE prod rate limit (50 certs/week, 5 duplicate-cert/week) | mitigate | D-05 two-commit pattern AND CADDY_ACME_CA staging default in .env.example. Caddy `caddy_data` named volume (preserved in docker-compose.prod.yml) persists cert state across container restarts per Pattern 2 — `docker compose down -v` is forbidden by Plan 04 runbook + Plan 02 verifier (assertion-only, no compose teardown). |
| T-06-01-04 | Spoofing | Caddyfile interpolation of operator-supplied CADDY_DOMAIN | accept | CADDY_DOMAIN is authored by the operator into the VM's .env from RUNBOOK §9 (Plan 04). Threat model assumes operator integrity; outside Phase 6 scope. |
| T-06-01-SC | Tampering | Package installs (npm/pip/cargo) | n/a | Plan 01 introduces ZERO new npm/pip/cargo packages — only edits to existing files. Per 06-RESEARCH.md §Package Legitimacy Audit: only apt + Docker Hub official images are added in this phase, both blessed. No blocking-human checkpoint needed for Plan 01. |
</threat_model>

<verification>
- `docker run --rm -v "$(pwd)/infra/Caddyfile:/etc/caddy/Caddyfile:ro" caddy:alpine caddy validate --config /etc/caddy/Caddyfile --adapter caddyfile` exits 0
- `docker compose -f infra/docker-compose.prod.yml config -q` exits 0 with various CADDY_ACME_CA env values (unset, empty, full staging URL)
- `bash infra/scripts/env-coverage.sh` (Phase 4 verifier) still exits 0 — CADDY_ACME_CA is intentionally NOT a FastAPI Settings field so it does not appear in the coverage check
- `bash infra/scripts/smoke.sh up` against the modified compose file still brings the laptop tls-internal stack up (regression guard for Phase 4)
- Note: Full HSTS + LE-prod-cert observability happens in Plan 05b (on the live VM after the prod-cert flip), not in this plan
</verification>

<success_criteria>
- All 3 modified files are committed in a single logical commit titled `feat(06): add acme_ca + HSTS to Caddyfile, thread CADDY_ACME_CA through compose env`
- Caddy validates the Caddyfile cleanly
- Compose config lints cleanly
- Phase 4 laptop bring-up regression-safe: `CADDY_DOMAIN=localhost` + `CADDY_TLS_MODE=internal` + `CADDY_ACME_CA=` (empty) still triggers tls internal because tls directive takes precedence and Caddy ignores acme_ca when tls internal is chosen
- DOMAIN-02 wiring complete (acme_ca env-var hook in place; actual LE issuance happens in Plan 05a/05b on the VM)
- DOMAIN-03 wiring complete (HSTS directive in Caddyfile; actually emitted by Caddy after prod-cert flip in Plan 05b)
- DOMAIN-04 partial: .env.example sentinel narrowed; actual `.env` narrowing on VM happens in Plan 05a
</success_criteria>

<output>
Create `.planning/phases/06-provision-oracle-vm-domain-caddy-https/06-01-SUMMARY.md` when done.
</output>
