---
phase: 06-provision-oracle-vm-domain-caddy-https
plan: 07
type: execute
wave: 5
depends_on:
  - 06-06
files_modified:
  - infra/runbook-evidence/uptime-alert.png
autonomous: false
requirements:
  - OPS-02
  - DEPLOY-05

tags:
  - uptimerobot
  - monitoring
  - sc5
  - alert-drill

must_haves:
  truths:
    - "An UptimeRobot account exists for the operator with the alert email verified"
    - "A single HTTPS monitor exists pointing at https://<your-domain>/health with 5-minute interval (per D-23 — no separate TLS-cert-expiry or root-URL monitors)"
    - "The monitor was deliberately tripped via `docker compose stop caddy` and triggered a real alert email to the operator's inbox"
    - "A screenshot of the alert email is committed at infra/runbook-evidence/uptime-alert.png"
    - "Caddy was restored via `docker compose start caddy` (NOT down -v) and UptimeRobot dashboard returned to Up within one cycle"
    - "Final `bash infra/scripts/verify-phase6.sh full <your-domain> <vm-ip>` exits 0 — all SC1–SC5 conditions observably satisfied"
  artifacts:
    - path: "infra/runbook-evidence/uptime-alert.png"
      provides: "SC5 alert-email screenshot — the OPS-02 evidence artifact required by ROADMAP SC5 and D-22"
      contains: ""
      min_bytes: 1024
  key_links:
    - from: "UptimeRobot probe servers (external)"
      to: "https://<your-domain>/health"
      via: "5-minute HTTPS GET via Cloudflare-DNS-resolved IP"
      pattern: "/health"
    - from: "UptimeRobot alert engine"
      to: "Operator's verified email address"
      via: "Email channel (free tier only — no SMS/webhook per D-21)"
      pattern: "Down alert"
---

<objective>
Operator-driven execution of RUNBOOK.md §14–§16: UptimeRobot signup + monitor creation (D-21), deliberate downtime drill to capture the SC5 alert-email screenshot (D-22), and final `verify-phase6.sh full` invocation as the holistic SC1–SC5 gate.

Purpose: Closes the OPS-02 / SC5 requirement that distinguishes "we hope the monitor works" from "we have observed the monitor work" — a real downtime triggered a real alert email and we have evidence. Final verifier invocation is the holistic gate that catches any regression introduced by Plans 05 and 06.

Output: 1 PNG screenshot committed to repo (the only file in `infra/runbook-evidence/` that is normally gitignored — operator explicitly git-adds it to override). After this plan: Phase 6 is complete, all 7 phase requirements satisfied, all 5 ROADMAP success criteria observable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-provision-oracle-vm-domain-caddy-https/06-CONTEXT.md
@.planning/phases/06-provision-oracle-vm-domain-caddy-https/06-RESEARCH.md
@infra/RUNBOOK.md
@infra/scripts/verify-phase6.sh
@.gitignore

<interfaces>
<!-- This plan depends on Plan 06 (LE prod cert in place) — UptimeRobot probes via HTTPS would error against staging cert. -->

D-21: UptimeRobot signup happens during Phase 6. Free tier. Single HTTPS monitor on /health, 5-minute interval, email alert (no SMS/webhook).
D-22: Deliberate downtime drill — `docker compose stop caddy`, wait for alert email, screenshot it to `infra/runbook-evidence/uptime-alert.png` (committed despite gitignore *.png rule — see below), then `docker compose start caddy`.
D-23: Single /health monitor only — no separate TLS-cert-expiry monitor (Caddy auto-renews) and no root-URL monitor.

.gitignore (from Plan 02): `infra/runbook-evidence/*.png` is ignored, BUT operators can override with `git add -f` to commit a specific PNG. The intent of the gitignore rule is to prevent ACCIDENTAL screenshot commits (e.g., debug screenshots during operator work); a deliberate `git add -f` for uptime-alert.png is the supported path. Plan 02's .gitkeep negation is for the directory marker only — operators must explicitly `git add -f infra/runbook-evidence/uptime-alert.png` to commit the SC5 evidence.

(Plan 02's existing rule is `infra/runbook-evidence/*.png` + `!infra/runbook-evidence/.gitkeep`. The .png is ignored by default; `-f` is the override.)

`bash infra/scripts/verify-phase6.sh full <your-domain> <vm-ip>` (from Plan 02) asserts all SC1–SC5 conditions including `test -s infra/runbook-evidence/uptime-alert.png` — so the screenshot must exist AND be non-empty AND be tracked/committed for the verifier to find it locally.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Operator executes RUNBOOK §14 — UptimeRobot signup + create /health monitor</name>
  <files>
    (External — UptimeRobot dashboard; no repo files modified)
  </files>
  <read_first>
    - infra/RUNBOOK.md §14 (UptimeRobot signup + monitor)
    - .planning/phases/06-provision-oracle-vm-domain-caddy-https/06-CONTEXT.md D-21, D-23
    - .planning/phases/06-provision-oracle-vm-domain-caddy-https/06-RESEARCH.md §"Pitfall 6" (UptimeRobot DNS resolver lag)
  </read_first>
  <what-built>
    Operator-driven dashboard click-through:

    a. Sign up at `https://uptimerobot.com` (free tier — no credit card required).
    b. Verify the welcome email (UptimeRobot sends an inbound verification link).
    c. Dashboard → Add New Monitor:
       - Monitor Type: **HTTPS**
       - Friendly Name: `wallet-app prod /health`
       - URL: `https://<your-domain>/health`
       - Monitoring Interval: **5 minutes** (free-tier minimum)
       - Alert Contacts: select the verified email address (click the verification link from UptimeRobot's separate "Please verify" email to enable that destination if not already done)
    d. Save the monitor.
    e. Wait 5–10 minutes for the first probe cycle (Pitfall 6 — UptimeRobot probe servers may need to resolve the DNS A record).
    f. Confirm the monitor shows status `Up` in the dashboard.

    Per D-23: NO additional monitors are created — no TLS-cert-expiry monitor, no root-URL monitor. Single /health monitor only.
  </what-built>
  <how-to-verify>
    Operator confirms (and pastes evidence in the resume message):
    1. UptimeRobot dashboard URL where the monitor is visible (e.g., `https://uptimerobot.com/dashboard?status=2`).
    2. Description of the monitor row visible in the dashboard: name, URL, interval, current status (must be `Up`).
    3. Confirmation that the alert email channel is verified (UptimeRobot dashboard → My Settings → Alert Contacts → email shows status `Active` or similar).
    4. Confirmation that only ONE monitor exists in the account (D-23 single-monitor requirement).
  </how-to-verify>
  <resume-signal>
    Reply with one of:
    - `approved` (paste the 4 verification outputs)
    - `needs help: <describe the blocker>` — common blockers: monitor shows Down immediately (Pitfall 6 — wait 10 min; if persists, verify `curl https://<your-domain>/health` works from laptop); UptimeRobot rejects the URL (verify Plan 06 prod cert is real, not staging — UptimeRobot's TLS validator may reject staging certs)
  </resume-signal>
  <acceptance_criteria>
    - UptimeRobot account active with verified email channel
    - Exactly one HTTPS monitor pointing at `https://<your-domain>/health` with 5-min interval
    - Monitor status shows Up after the first full check cycle
    - D-21 + D-23 satisfied
  </acceptance_criteria>

  <action>
    Operator-driven checkpoint task. Follow the steps documented in <what-built> above and the referenced RUNBOOK sections in <read_first>. This is not an executor-agent task — the agent presents the checkpoint, the operator performs the steps on Oracle Cloud / Cloudflare / their laptop / the VM, then replies per <resume-signal>. The executor agent records the operator-provided evidence (pasted command outputs, file contents, descriptions) in the plan SUMMARY when the checkpoint is approved.
  </action>
  <verify>
    <human-check>Operator confirms (and pastes evidence in the resume message):
    1. UptimeRobot dashboard URL where the monitor is visible (e.g., `https://uptimerobot.com/dashboard?status=2`).
    2. Description of the monitor row visible in the dashboard: name, URL, interval, current status (must be `Up`).
    3. Confirmation that the alert email channel is verified (UptimeRobot dashboard → My Settings → Alert Contacts → email shows status `Active` or similar).
    4. Confirmation that only ONE monitor exists in the account (D-23 single-monitor requirement).</human-check>
  </verify>
  <done>- UptimeRobot account active with verified email channel
    - Exactly one HTTPS monitor pointing at `https://<your-domain>/health` with 5-min interval
    - Monitor status shows Up after the first full check cycle
    - D-21 + D-23 satisfied</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator executes RUNBOOK §15 — Deliberate downtime drill + screenshot SC5 evidence</name>
  <files>
    Committed to repo (requires git add -f to override .gitignore):
      infra/runbook-evidence/uptime-alert.png
  </files>
  <read_first>
    - infra/RUNBOOK.md §15 (Deliberate downtime drill)
    - .planning/phases/06-provision-oracle-vm-domain-caddy-https/06-CONTEXT.md D-22
    - .planning/phases/06-provision-oracle-vm-domain-caddy-https/06-RESEARCH.md §"Pitfall 8" (no down -v)
  </read_first>
  <what-built>
    Operator-driven sequence:

    a. Baseline: UptimeRobot dashboard shows monitor as Up.

    b. On VM as `deploy`, stop caddy (only — preserves api/db so /health would still work locally; only the external path is broken):
    ```bash
    cd ~/wallet_app/infra
    docker compose -f docker-compose.prod.yml stop caddy
    docker compose -f docker-compose.prod.yml ps caddy
    # Expected: caddy state = exited
    ```

    c. Wait up to 10 minutes for UptimeRobot to detect 2 consecutive failures (free-tier alert threshold). The alert email arrives in the operator's verified inbox during this window. Operator notes the timestamp the email arrived.

    d. Take a screenshot of the alert email. The screenshot must clearly show:
       - The "Down" alert subject line
       - The monitor name (`wallet-app prod /health`)
       - The timestamp of the down detection
       - The body of the email referencing the monitor URL
       (Operator may redact personal email metadata if desired; the SC5 artifact is the alert itself, not the recipient identity.)

    e. Save the screenshot to `infra/runbook-evidence/uptime-alert.png` (PNG format; <1 MB preferred to keep repo light).

    f. Restore caddy (NOT down -v — Pitfall 8):
    ```bash
    docker compose -f docker-compose.prod.yml start caddy
    docker compose -f docker-compose.prod.yml ps caddy
    sleep 30  # let caddy stabilize
    curl -sf "https://<your-domain>/health" | jq .
    # Expected: {"status":"healthy",...}
    ```

    g. Wait one full UptimeRobot check cycle (5 min). Confirm dashboard returns to Up status.

    h. Commit the screenshot to the repo (override .gitignore via `git add -f`):
    ```bash
    git add -f infra/runbook-evidence/uptime-alert.png
    git commit -m "evidence(06): UptimeRobot alert email captured during deliberate downtime drill (SC5 / OPS-02)"
    ```
  </what-built>
  <how-to-verify>
    Operator confirms (and pastes evidence in the resume message):
    1. The screenshot file exists at `infra/runbook-evidence/uptime-alert.png` (paste `ls -la infra/runbook-evidence/uptime-alert.png`).
    2. The screenshot file is non-empty (paste `stat -c '%s' infra/runbook-evidence/uptime-alert.png` showing >= 1024 bytes).
    3. The screenshot file is committed to git (paste `git ls-files infra/runbook-evidence/uptime-alert.png` showing the path).
    4. Description of the screenshot contents: confirm it shows the UptimeRobot Down alert subject line, monitor name (`wallet-app prod /health`), and a down-detection timestamp consistent with the drill.
    5. UptimeRobot dashboard description after restore: the monitor shows Up; the historical view shows the brief downtime block during the drill.
    6. From laptop: `curl -sf "https://<your-domain>/health" | jq .` shows healthy after restore.
    7. On VM: `docker volume ls | grep caddy_data` confirms the volume is still present (Pitfall 8 not triggered).
  </how-to-verify>
  <resume-signal>
    Reply with one of:
    - `approved` (paste the 7 verification outputs as evidence; the screenshot description is the critical SC5 confirmation)
    - `needs help: <describe the blocker>` — common blockers: alert email did not arrive (verify alert contact is confirmed in UptimeRobot dashboard; check spam folder); screenshot too large for repo (compress or crop); git add -f rejected (re-check the gitignore rule with `git check-ignore -v infra/runbook-evidence/uptime-alert.png`); UptimeRobot doesn't return to Up after restore (check caddy logs, verify port 443 reachable)
  </resume-signal>
  <acceptance_criteria>
    - `infra/runbook-evidence/uptime-alert.png` exists, is non-empty (>= 1 KB), and is tracked + committed in git
    - The image content visually confirms an UptimeRobot Down alert
    - Monitor returned to Up after restore
    - caddy_data volume preserved
    - D-22 + SC5 satisfied
  </acceptance_criteria>

  <action>
    Operator-driven checkpoint task. Follow the steps documented in <what-built> above and the referenced RUNBOOK sections in <read_first>. This is not an executor-agent task — the agent presents the checkpoint, the operator performs the steps on Oracle Cloud / Cloudflare / their laptop / the VM, then replies per <resume-signal>. The executor agent records the operator-provided evidence (pasted command outputs, file contents, descriptions) in the plan SUMMARY when the checkpoint is approved.
  </action>
  <verify>
    <human-check>Operator confirms (and pastes evidence in the resume message):
    1. The screenshot file exists at `infra/runbook-evidence/uptime-alert.png` (paste `ls -la infra/runbook-evidence/uptime-alert.png`).
    2. The screenshot file is non-empty (paste `stat -c '%s' infra/runbook-evidence/uptime-alert.png` showing >= 1024 bytes).
    3. The screenshot file is committed to git (paste `git ls-files infra/runbook-evidence/uptime-alert.png` showing the path).
    4. Description of the screenshot contents: confirm it shows the UptimeRobot Down alert subject line, monitor name (`wallet-app prod /health`), and a down-detection timestamp consistent with the drill.
    5. UptimeRobot dashboard description after restore: the monitor shows Up; the historical view shows the brief downtime block during the drill.
    6. From laptop: `curl -sf "https://<your-domain>/health" | jq .` shows healthy after restore.
    7. On VM: `docker volume ls | grep caddy_data` confirms the volume is still present (Pitfall 8 not triggered).</human-check>
  </verify>
  <done>- `infra/runbook-evidence/uptime-alert.png` exists, is non-empty (>= 1 KB), and is tracked + committed in git
    - The image content visually confirms an UptimeRobot Down alert
    - Monitor returned to Up after restore
    - caddy_data volume preserved
    - D-22 + SC5 satisfied</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Operator executes RUNBOOK §16 — Final verify-phase6.sh full as SC1–SC5 holistic gate</name>
  <files>
    Reads-only:
      infra/scripts/verify-phase6.sh
    (No new files modified)
  </files>
  <read_first>
    - infra/RUNBOOK.md §16 (Final verification script)
    - infra/scripts/verify-phase6.sh (Plan 02's verifier)
    - .planning/phases/06-provision-oracle-vm-domain-caddy-https/06-VALIDATION.md §Per-Requirement Verification Map
  </read_first>
  <what-built>
    Final verifier invocation as the holistic SC1–SC5 gate. Operator runs both modes from their laptop (or any machine with `docker`, `dig`, `curl`, `nmap`, and a checkout of the repo with the just-committed `uptime-alert.png`):

    a. Quick mode (local static-file assertions):
    ```bash
    cd <path-to>/wallet_app
    bash infra/scripts/verify-phase6.sh quick
    # Expected last line: OK: Phase 6 quick verify green
    ```

    b. Full mode (against live VM — all SC1–SC5):
    ```bash
    bash infra/scripts/verify-phase6.sh full <your-domain> <vm-ip>
    # Expected last line: OK: Phase 6 full verify green
    ```

    Each assertion within full mode maps to a ROADMAP success criterion:
    - DNS resolves → DOMAIN-01 / SC1
    - HTTPS 200 → SC1
    - LE prod cert (not STAGING) → DOMAIN-02 / SC1
    - HSTS header → DOMAIN-03 / SC5
    - nmap 22 closed, 23333/80/443 open → SEC-02 / SC3
    - /health 200 + JSON → OPS-01 / SC4
    - /health < 500ms → OPS-01 / SC4
    - uptime-alert.png exists → OPS-02 / SC5

    If any assertion fails, the FAIL: <reason> message identifies the failing condition. Operator addresses the gap and re-runs.
  </what-built>
  <how-to-verify>
    Operator confirms (and pastes evidence in the resume message):
    1. Paste the full stdout from `bash infra/scripts/verify-phase6.sh quick` — must end with `OK: Phase 6 quick verify green`.
    2. Paste the full stdout from `bash infra/scripts/verify-phase6.sh full <your-domain> <vm-ip>` — must end with `OK: Phase 6 full verify green`.
    3. Confirm exit code: paste `echo $?` immediately after the full-mode run — must be `0`.
    4. Cross-reference: enumerate the 8 full-mode `==>` step lines from the output and confirm each maps to a ROADMAP SC (SC1: DNS + HTTPS 200 + LE prod cert; SC2: implicit via RUNBOOK completion; SC3: nmap; SC4: /health DB-independence via §12 + /health 200 from script; SC5: HSTS + uptime-alert.png).
  </how-to-verify>
  <resume-signal>
    Reply with one of:
    - `approved` (paste the 4 verification outputs as evidence — the verbatim verifier stdout for both modes is the keystone)
    - `needs help: <FAIL: ... reason from script output>` — match the FAIL message to the relevant section of the RUNBOOK and re-execute that step. Common gaps: HSTS header missing (verify Plan 01's Caddyfile edit deployed — `docker compose exec caddy cat /etc/caddy/Caddyfile | grep -i strict`); LE staging cert still showing (Plan 06 restart did not pick up the .env change — verify `docker compose exec caddy printenv CADDY_ACME_CA` returns empty); nmap shows port 22 open (verify ssh.socket override applied correctly from Plan 05 Task 1 — `sudo ss -tlnp | grep ssh` should show 23333, not 22)
  </resume-signal>
  <acceptance_criteria>
    - `verify-phase6.sh quick` exits 0 with success marker
    - `verify-phase6.sh full <your-domain> <vm-ip>` exits 0 with success marker
    - All 8 full-mode assertions pass
    - Phase 6 is observably complete: SC1, SC2, SC3, SC4, SC5 all green
    - All 7 requirements (DEPLOY-05, DOMAIN-01, DOMAIN-02, DOMAIN-03, SEC-02, OPS-01, OPS-02) observable
  </acceptance_criteria>

  <action>
    Operator-driven checkpoint task. Follow the steps documented in <what-built> above and the referenced RUNBOOK sections in <read_first>. This is not an executor-agent task — the agent presents the checkpoint, the operator performs the steps on Oracle Cloud / Cloudflare / their laptop / the VM, then replies per <resume-signal>. The executor agent records the operator-provided evidence (pasted command outputs, file contents, descriptions) in the plan SUMMARY when the checkpoint is approved.
  </action>
  <verify>
    <human-check>Operator confirms (and pastes evidence in the resume message):
    1. Paste the full stdout from `bash infra/scripts/verify-phase6.sh quick` — must end with `OK: Phase 6 quick verify green`.
    2. Paste the full stdout from `bash infra/scripts/verify-phase6.sh full <your-domain> <vm-ip>` — must end with `OK: Phase 6 full verify green`.
    3. Confirm exit code: paste `echo $?` immediately after the full-mode run — must be `0`.
    4. Cross-reference: enumerate the 8 full-mode `==>` step lines from the output and confirm each maps to a ROADMAP SC (SC1: DNS + HTTPS 200 + LE prod cert; SC2: implicit via RUNBOOK completion; SC3: nmap; SC4: /health DB-independence via §12 + /health 200 from script; SC5: HSTS + uptime-alert.png).</human-check>
  </verify>
  <done>- `verify-phase6.sh quick` exits 0 with success marker
    - `verify-phase6.sh full <your-domain> <vm-ip>` exits 0 with success marker
    - All 8 full-mode assertions pass
    - Phase 6 is observably complete: SC1, SC2, SC3, SC4, SC5 all green
    - All 7 requirements (DEPLOY-05, DOMAIN-01, DOMAIN-02, DOMAIN-03, SEC-02, OPS-01, OPS-02) observable</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UptimeRobot probe servers → VM port 443 | External monitor; threat is monitor misconfig (wrong URL → false positives) — mitigated by Task 1 verification |
| Alert email channel | Email delivery is a third-party concern; threat is alert not received → SC5 cannot be evidenced — accept (operator can recreate the drill if first attempt fails) |
| `docker compose stop caddy` → caddy_data | Stop is volume-safe; threat is operator typing `down -v` instead — mitigated by Task 2's explicit command in `<what-built>` and post-restore volume check |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07-01 | Repudiation | SC5 evidence missing or fake | mitigate | Task 2 verification step 3 confirms screenshot is git-tracked (i.e., a real file in the repo, not just a placeholder). Step 4 requires operator to describe visible contents — at least subject line and monitor name. |
| T-06-07-02 | Denial of Service | caddy_data wipe via wrong restore command | mitigate | Task 2 `<what-built>` step f explicitly uses `start caddy` (volume-safe). Step 7 of verification asserts `docker volume ls | grep caddy_data` after the restore. |
| T-06-07-03 | Spoofing | Fake alert email or screenshot | accept | Operator-attested. The SC5 evidence is a "process was completed" artifact, not a forensic record; the threat model assumes operator integrity here. |
| T-06-07-04 | Information Disclosure | Screenshot includes operator's personal email metadata | accept | RUNBOOK §15 + Task 2 `<what-built>` explicitly permits redacting personal email metadata (recipient address, sender domain) — the artifact value is the alert content, not the email envelope. |
| T-06-07-05 | Tampering | UptimeRobot configuration drift after drill (operator deletes monitor) | accept | OPS-02 verification is point-in-time (during this drill). Long-term monitoring health is operator responsibility; Phase 7 may add monitoring checks to CI. |
| T-06-07-SC | Tampering | apt/package installs | n/a | No new packages introduced in this plan. |
</threat_model>

<verification>
- Task 1, 2, 3 checkpoints approved by operator with pasted evidence
- `infra/runbook-evidence/uptime-alert.png` committed to repo
- `verify-phase6.sh full <your-domain> <vm-ip>` exits 0 with all 8 assertions passing
- All 7 Phase 6 requirements (DEPLOY-05, DOMAIN-01, DOMAIN-02, DOMAIN-03, SEC-02, OPS-01, OPS-02) observably satisfied
- All 5 ROADMAP success criteria (SC1, SC2, SC3, SC4, SC5) observably satisfied
- Phase 6 complete; ready to hand off to `/gsd:verify-work` and then Phase 7 planning
</verification>

<success_criteria>
- UptimeRobot monitor active, alert drill executed, SC5 evidence captured
- Final verifier (quick + full modes) green
- Phase 6 holistically complete
- Hand-off ready: `/gsd:verify-work 06` runs cleanly, Phase 7 can begin
</success_criteria>

<output>
Create `.planning/phases/06-provision-oracle-vm-domain-caddy-https/06-07-SUMMARY.md` when done.
</output>
