---
plan_id: 05-06
phase: 05
plan: 06
type: execute
wave: 1
depends_on: ["05-01"]
files_modified:
  - frontend/src/screens/TransactionFormScreen.tsx
requirements:
  - MOBUI-03
  - MOBUI-04
autonomous: true
must_haves:
  truths:
    - "Tapping the transaction amount input field on iOS Safari mobile does NOT trigger a zoom-on-focus, because the global `input, select, textarea { font-size: 16px !important; }` rule from `frontend/public/pwa.css` applies (MOBUI-03 — handled in 05-02 + the bundling chain in 05-03)."
    - "The transaction amount field renders an HTML attribute `inputmode='decimal'` on web (in addition to the existing `keyboardType='decimal-pad'` which RN-web maps to the same attribute — belt-and-braces per MOBUI-04 and RESEARCH § Don't Hand-Roll)."
    - "The email fields on LoginScreen.tsx and RegisterScreen.tsx already declare `keyboardType='email-address'` AND `autoComplete='email'` (verified by `verify-touch-targets.mjs --check=login-email-props` and `--check=register-email-props`) — no edit required for those files per RESEARCH § Pitfall 6."
    - "Running `node frontend/scripts/verify-touch-targets.mjs --check=tx-amount-inputmode` emits `PASS: tx-amount-inputmode` and exits 0; the same holds for `--check=login-email-props` and `--check=register-email-props` after this plan."
    - "Running `node frontend/scripts/verify-touch-targets.mjs --all` exits 0 with `==> all touch-target checks passed` — the phase-wide aggregate gate proving every check in the ten-check registry (button-small-44, 3 spot-fix [finance/tx/profile], tx-amount-inputmode, login/register-email-props, screen-safe-area, app-safe-area-provider, all-touchables) is green."
  artifacts:
    - path: "frontend/src/screens/TransactionFormScreen.tsx"
      provides: "Three amount-field <TextInput>s each declare both keyboardType='decimal-pad' AND inputMode='decimal' (lines ~535, ~628, ~727)"
      contains: "inputMode=\"decimal\""
  key_links:
    - from: "frontend/src/screens/TransactionFormScreen.tsx amount fields"
      to: "react-native-web 0.21+ inputMode prop → HTML inputmode='decimal'"
      via: "JSX prop"
      pattern: "inputMode=\"decimal\""
---

<objective>
Land the MOBUI-04 fix in `frontend/src/screens/TransactionFormScreen.tsx` by adding an explicit `inputMode="decimal"` prop alongside the existing `keyboardType="decimal-pad"` on every amount field (3 sites confirmed by PATTERNS.md). Verify that MOBUI-03 (the iOS-zoom prevention CSS) is already serving its purpose via the `pwa.css` rule shipped in 05-02, and that the LoginScreen + RegisterScreen email fields are already MOBUI-04 compliant per RESEARCH § Pitfall 6.

Purpose: Per RESEARCH §Don't Hand-Roll + Open Question 2, `keyboardType="decimal-pad"` already maps to `inputmode="decimal"` in react-native-web 0.21+, but `inputMode` is the spec-correct, forward-compatible prop and it takes precedence on web. Explicitly declaring both is the MOBUI-04 contract. The LoginScreen/RegisterScreen email fields are already correct — touching them risks regression (RESEARCH § Pitfall 6: "Don't try to 'fix' what's already right").

Output: Three amount-field edits in TransactionFormScreen.tsx; `verify-touch-targets.mjs --check=tx-amount-inputmode` PASSes, `--check=login-email-props` and `--check=register-email-props` PASS, and the phase-wide `--all` aggregate gate exits 0 with `==> all touch-target checks passed`; no other source file is modified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-pwa-ify-frontend-mobile-polish/05-CONTEXT.md
@.planning/phases/05-pwa-ify-frontend-mobile-polish/05-UI-SPEC.md
@.planning/phases/05-pwa-ify-frontend-mobile-polish/05-RESEARCH.md
@.planning/phases/05-pwa-ify-frontend-mobile-polish/05-PATTERNS.md
@.planning/phases/05-pwa-ify-frontend-mobile-polish/05-01-SUMMARY.md
@frontend/src/screens/TransactionFormScreen.tsx
@frontend/src/screens/LoginScreen.tsx
@frontend/src/screens/RegisterScreen.tsx

<interfaces>
<!-- TransactionFormScreen amount-field locations (PATTERNS § TransactionFormScreen.tsx, line guidance from RESEARCH § Code Examples): -->
<!--   Line ~535: original (single-transaction) amount field -->
<!--   Line ~628: transfer-source amount field -->
<!--   Line ~727: transfer-dest amount field -->
<!-- Each currently declares: keyboardType="decimal-pad" -->
<!-- Each must additionally declare: inputMode="decimal" -->

<!-- LoginScreen.tsx (lines ~70-72 per PATTERNS § Pitfall 6, already correct): -->
<!--   keyboardType="email-address" -->
<!--   autoCapitalize="none" -->
<!--   autoComplete="email" -->

<!-- RegisterScreen.tsx (lines ~106-108, already correct): same three props. -->
<!-- DO NOT TOUCH these files — they already satisfy MOBUI-04. -->

<!-- MOBUI-03 (16px input font) is served via frontend/public/pwa.css (authored in 05-02) and copied to dist/pwa.css during expo export (05-02 Task 4 smoke). -->
<!-- It is verified end-to-end by `node scripts/verify-pwa.mjs --check=ios-zoom` (already green per 05-02). -->
<!-- This plan does NOT re-author pwa.css; it only verifies MOBUI-03 still holds and adds the MOBUI-04 inputMode props. -->

<!-- verify-touch-targets.mjs check-name registry (from 05-01 Task 4 — exact stable strings this plan greps): -->
<!--   tx-amount-inputmode      — TransactionFormScreen.tsx inputMode=decimal count matches keyboardType=decimal-pad count and both ≥3 -->
<!--   login-email-props        — LoginScreen.tsx declares keyboardType=email-address and autoComplete=email -->
<!--   register-email-props     — RegisterScreen.tsx declares keyboardType=email-address and autoComplete=email -->
<!-- Phase-wide aggregate gate: `--all` runs every check in the registry, collects failures (does NOT exit-on-first-failure), and exits 0 only if every check passes. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add inputMode="decimal" to all three amount-field TextInputs in TransactionFormScreen.tsx</name>
  <read_first>
    - frontend/src/screens/TransactionFormScreen.tsx (full file — locate the three TextInput sites that declare keyboardType="decimal-pad"; the exact line numbers in the source-of-record may differ slightly from PATTERNS.md's ~535/~628/~727 if the file has been edited between research and execution; use grep for confirmation)
    - .planning/phases/05-pwa-ify-frontend-mobile-polish/05-PATTERNS.md § TransactionFormScreen.tsx (the locked diff — three identical patches; the change at each site is `keyboardType="decimal-pad"` → `keyboardType="decimal-pad"` + a new sibling line `inputMode="decimal"`)
    - .planning/phases/05-pwa-ify-frontend-mobile-polish/05-RESEARCH.md § Code Examples § TransactionFormScreen.tsx amount field
    - .planning/phases/05-pwa-ify-frontend-mobile-polish/05-RESEARCH.md § Don't Hand-Roll (the decimal-pad → inputmode auto-map rationale; we still add the explicit prop for forward-compat)
    - frontend/scripts/verify-touch-targets.mjs check `tx-amount-inputmode` (the assertion: count of `keyboardType="decimal-pad"` must equal count of `inputMode="decimal"` and both ≥3)
  </read_first>
  <behavior>
    - After edit, the count of `keyboardType="decimal-pad"` occurrences in `TransactionFormScreen.tsx` matches the count of `inputMode="decimal"` occurrences (both should be 3, OR both could be the same N if the file has more amount fields than PATTERNS predicted).
    - Each `inputMode="decimal"` declaration sits on the same TextInput element as its paired `keyboardType="decimal-pad"` (sibling JSX props on the same element, not on different elements).
    - No other prop on those TextInputs changes (`value`, `onChangeText`, `placeholder`, validation hookup, etc. all remain).
    - `verify-touch-targets.mjs --check=tx-amount-inputmode` PASSES.
  </behavior>
  <files>frontend/src/screens/TransactionFormScreen.tsx</files>
  <action>
    Open `frontend/src/screens/TransactionFormScreen.tsx`. For EVERY occurrence of the literal substring `keyboardType="decimal-pad"`, add a sibling JSX prop on the same TextInput element: `inputMode="decimal"`. The new prop may go on the line immediately above OR below the `keyboardType` line — executor's choice for cleanest diff, but it MUST be on the same TextInput (between the same opening `<TextInput` and the JSX close).

    Do NOT change any other prop. Do NOT change any of the three amount-field's `value`, `onChangeText`, `placeholder`, `keyboardType`, validation imports, or styling. Do NOT touch the currency-trigger TouchableOpacity or the currency-row modal styles (those belong to plan 05-04 and are already done by the time this plan runs in Wave 1).

    Confirm by counting occurrences: `grep -c 'keyboardType="decimal-pad"' src/screens/TransactionFormScreen.tsx` must equal `grep -c 'inputMode="decimal"' src/screens/TransactionFormScreen.tsx` after the edit.
  </action>
  <verify>
    <automated>cd frontend && a=$(grep -c 'keyboardType="decimal-pad"' src/screens/TransactionFormScreen.tsx); b=$(grep -c 'inputMode="decimal"' src/screens/TransactionFormScreen.tsx); test "$a" = "$b" -a "$a" -ge 3 && node scripts/verify-touch-targets.mjs --check=tx-amount-inputmode 2>&1 | grep -qE "^PASS: tx-amount-inputmode$" && echo "==> inputMode count ($b) matches keyboardType count ($a), ≥3; verify-touch-targets[tx-amount-inputmode] PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/screens/TransactionFormScreen.tsx` `grep -c 'keyboardType="decimal-pad"'` equals `grep -c 'inputMode="decimal"'` AND both are ≥3.
    - Each occurrence of `inputMode="decimal"` is on a TextInput that also declares `keyboardType="decimal-pad"` (verified by reading the surrounding JSX — no orphan `inputMode` on a non-decimal field).
    - `cd frontend && node scripts/verify-touch-targets.mjs --check=tx-amount-inputmode` exits 0 AND stdout contains the literal line `PASS: tx-amount-inputmode`.
    - `git diff src/screens/TransactionFormScreen.tsx` shows ONLY additions of `inputMode="decimal"` lines — no deletions of other props, no modifications to validation logic, no style changes.
  </acceptance_criteria>
  <done>Three (or more, if the file has additional amount fields) inputMode props added; counts match; `verify-touch-targets[tx-amount-inputmode]` PASS.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verify LoginScreen + RegisterScreen email fields remain MOBUI-04 compliant (no edits)</name>
  <read_first>
    - frontend/src/screens/LoginScreen.tsx (lines ~70-72 — confirm the three email-field props are already present)
    - frontend/src/screens/RegisterScreen.tsx (lines ~106-108 — same)
    - .planning/phases/05-pwa-ify-frontend-mobile-polish/05-RESEARCH.md § Pitfall 6 ("Don't try to 'fix' what's already right")
    - frontend/scripts/verify-touch-targets.mjs checks `login-email-props` and `register-email-props`
  </read_first>
  <behavior>
    - `LoginScreen.tsx` source contains the literal substrings `keyboardType="email-address"` AND `autoComplete="email"`.
    - `RegisterScreen.tsx` source contains the same two literal substrings.
    - Neither file is modified by this plan.
    - `verify-touch-targets.mjs --check=login-email-props` and `--check=register-email-props` both PASS against the current (already-correct) source.
  </behavior>
  <files>(no edits; verification only)</files>
  <action>
    Run the following non-destructive checks from `frontend/`:

    ```
    grep -F 'keyboardType="email-address"' src/screens/LoginScreen.tsx
    grep -F 'autoComplete="email"' src/screens/LoginScreen.tsx
    grep -F 'keyboardType="email-address"' src/screens/RegisterScreen.tsx
    grep -F 'autoComplete="email"' src/screens/RegisterScreen.tsx
    node scripts/verify-touch-targets.mjs --check=login-email-props
    node scripts/verify-touch-targets.mjs --check=register-email-props
    ```

    All six MUST succeed (the four greps exit 0, both verify-touch-targets invocations emit `PASS: <name>` and exit 0). If any fails — i.e. a previous phase regressed the email-field props — DO NOT silently fix it here. Instead, the task fails: this is a discovery of a regression in a prior phase that needs its own gap-closure plan. Log the failure to the SUMMARY for follow-up. Per RESEARCH § Pitfall 6, the codebase shape is already correct as of the research date (2026-05-23) and this plan trusts that contract.
  </action>
  <verify>
    <automated>cd frontend && grep -F 'keyboardType="email-address"' src/screens/LoginScreen.tsx && grep -F 'autoComplete="email"' src/screens/LoginScreen.tsx && grep -F 'keyboardType="email-address"' src/screens/RegisterScreen.tsx && grep -F 'autoComplete="email"' src/screens/RegisterScreen.tsx && node scripts/verify-touch-targets.mjs --check=login-email-props 2>&1 | grep -qE "^PASS: login-email-props$" && node scripts/verify-touch-targets.mjs --check=register-email-props 2>&1 | grep -qE "^PASS: register-email-props$" && ! git diff --stat src/screens/LoginScreen.tsx src/screens/RegisterScreen.tsx | grep -E "[1-9]" && echo "==> Email props confirmed compliant; verify-touch-targets[login-email-props] + [register-email-props] PASS; no edits"</automated>
  </verify>
  <acceptance_criteria>
    - Both `LoginScreen.tsx` and `RegisterScreen.tsx` contain both `keyboardType="email-address"` AND `autoComplete="email"`.
    - `cd frontend && node scripts/verify-touch-targets.mjs --check=login-email-props` exits 0 AND stdout contains `PASS: login-email-props`.
    - `cd frontend && node scripts/verify-touch-targets.mjs --check=register-email-props` exits 0 AND stdout contains `PASS: register-email-props`.
    - `git diff` shows no changes to either file under this plan.
  </acceptance_criteria>
  <done>Email fields verified compliant via stable per-check verifier; no file edits made in this task.</done>
</task>

<task type="auto">
  <name>Task 3: Phase-wide aggregate gate — verify-touch-targets.mjs --all + verify-pwa.mjs --all + npm test all green</name>
  <read_first>
    - frontend/scripts/verify-touch-targets.mjs (full file — all ten checks in the registry; `--all` runs every check and aggregates failures, does NOT exit on first failure)
    - frontend/scripts/verify-pwa.mjs (the --check=ios-zoom branch confirms MOBUI-03 via pwa.css)
    - .planning/phases/05-pwa-ify-frontend-mobile-polish/05-VALIDATION.md § Sampling Rate (wave-merge gate)
  </read_first>
  <files>(no edits; final gate for Wave 1)</files>
  <action>
    From `frontend/`, run the full Wave-1 verification battery (assumes 05-02, 05-03, 05-04, 05-05 have all landed):

    ```
    node scripts/verify-touch-targets.mjs --all   # All ten registry checks green; output ends with `==> all touch-target checks passed`
    node scripts/verify-pwa.mjs --all             # All five PWA checks (manifest, apple-meta, viewport, sw, ios-zoom) green
    npm test                                       # Full Jest suite (Button.test.ts + 3 existing specs)
    ```

    All three commands MUST exit 0. The verify-touch-targets `--all` mode aggregates failures across the registry — if any check FAILs, the aggregated `FAIL: <check-name> — <reason>` lines on stderr name the failing check(s) without ambiguity:
    - `FAIL: tx-amount-inputmode — ...` → return to Task 1 (counts mismatched)
    - `FAIL: login-email-props — ...` or `FAIL: register-email-props — ...` → regression in LoginScreen/RegisterScreen; route to a gap-closure plan
    - `FAIL: button-small-44 — ...` or `FAIL: finance-form-spot-fix-44 — ...` / `tx-form-spot-fix-44` / `profile-spot-fix-44` → 05-04 has not fully landed; route back
    - `FAIL: screen-safe-area — ...` or `FAIL: app-safe-area-provider — ...` → 05-05 has not fully landed; route back
    - `FAIL: all-touchables — ...` → 05-04 Task 6 (full grep audit) has not fully landed OR a new touchable was introduced without `minHeight: 44` / `// touch-target-exempt:` marker; route back to 05-04 Task 6

    If verify-pwa fails on `--check=ios-zoom`, the `dist/pwa.css` rule was dropped — verify that the latest `npm run build:web` ran and that `frontend/public/pwa.css` still contains the `font-size: 16px !important` rule.
  </action>
  <verify>
    <automated>cd frontend && node scripts/verify-touch-targets.mjs --all 2>&1 | grep -qE "^==> all touch-target checks passed$" && node scripts/verify-pwa.mjs --all && npm test 2>&1 | tail -10 | grep -E "(Tests:.*passed|Test Suites:.*passed)" && echo "==> Wave 1 full battery green (touch-targets --all + PWA --all + Jest)"</automated>
  </verify>
  <acceptance_criteria>
    - `node frontend/scripts/verify-touch-targets.mjs --all` exits 0 AND stdout contains the literal line `==> all touch-target checks passed` (proving every check in the ten-check registry PASSed).
    - `node frontend/scripts/verify-pwa.mjs --all` exits 0 (all five PWA checks PASS).
    - `cd frontend && npm test` reports zero failing suites.
  </acceptance_criteria>
  <done>End of Wave 1 — verify-touch-targets `--all` aggregate gate is green (every check in the registry PASSes); verify-pwa `--all` is green; Jest suite passes; phase ready for the Wave 2 final gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a — purely additive HTML attribute changes | No new trust boundaries; the inputMode prop is a UX hint, not a security boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-01 | Tampering / data freshness | Stale-shell trap | accept | Mitigated by 05-03's SW (skipWaiting + clientsClaim silent takeover + NetworkFirst on `/index.html`, ROADMAP SC-3 preserved). Per revised D-07/D-08/D-09, no UpdateToast UI — silent takeover is the contract. |
| T-05-06-02 | Information Disclosure | Cross-user cache leakage | accept | Out of scope. |
| T-05-06-03 | Tampering / Spoofing | Service worker hijack | accept | HTTPS + same-origin (Phase 4). |
| T-05-06-04 | Tampering | Cache poisoning | accept | Mitigated by 05-03's Workbox config (cleanupOutdatedCaches + content-hashed URLs). |
| T-05-06-05 | Tampering | Reflected XSS via manifest | accept | No manifest edits. |
| T-05-06-06 | Input Validation | Decimal amount parsing on web | accept | The inputMode prop is a UX hint that surfaces a numeric keypad; server-side and existing client-side (zod) validation continues to enforce numeric format. The prop does not weaken validation. |
| T-05-06-SC | Tampering | Supply chain | accept | No npm installs. |
</threat_model>

<verification>
After all tasks in this plan complete:

```bash
cd frontend
# TransactionFormScreen inputMode counts balanced
a=$(grep -c 'keyboardType="decimal-pad"' src/screens/TransactionFormScreen.tsx)
b=$(grep -c 'inputMode="decimal"' src/screens/TransactionFormScreen.tsx)
test "$a" = "$b" -a "$a" -ge 3
# LoginScreen + RegisterScreen email fields verified untouched + compliant
grep -F 'autoComplete="email"' src/screens/LoginScreen.tsx
grep -F 'autoComplete="email"' src/screens/RegisterScreen.tsx
# MOBUI-04 per-check verifies PASS in isolation
node scripts/verify-touch-targets.mjs --check=tx-amount-inputmode | grep -qE "^PASS: tx-amount-inputmode$"
node scripts/verify-touch-targets.mjs --check=login-email-props | grep -qE "^PASS: login-email-props$"
node scripts/verify-touch-targets.mjs --check=register-email-props | grep -qE "^PASS: register-email-props$"
# Phase-wide --all aggregate gate (every check in the registry PASSes)
node scripts/verify-touch-targets.mjs --all | grep -qE "^==> all touch-target checks passed$"
node scripts/verify-pwa.mjs --all
npm test
```
</verification>

<success_criteria>
1. `frontend/src/screens/TransactionFormScreen.tsx` declares `inputMode="decimal"` on every TextInput that declares `keyboardType="decimal-pad"` (counts balanced, both ≥3).
2. `frontend/src/screens/LoginScreen.tsx` and `frontend/src/screens/RegisterScreen.tsx` remain unchanged and both still declare `keyboardType="email-address"` + `autoComplete="email"`.
3. `node frontend/scripts/verify-touch-targets.mjs --check=tx-amount-inputmode`, `--check=login-email-props`, and `--check=register-email-props` each exit 0 with their respective `PASS: <name>` line.
4. `node frontend/scripts/verify-touch-targets.mjs --all` exits 0 AND emits the literal aggregate-success line `==> all touch-target checks passed` (the phase-wide gate; proves every check in the ten-check registry PASSes).
5. `node frontend/scripts/verify-pwa.mjs --all` exits 0 — all five PWA static checks PASS (including --check=ios-zoom which verifies the pwa.css 16px rule).
6. `cd frontend && npm test` reports zero failing suites.
7. End of Wave 1: every requirement from PWA-01..04 and MOBUI-01..04 is satisfied by static-verification artifacts; only MOBUI-05 (real-device install) remains, and per CONTEXT D-13 it is explicitly deferred to Phase 6.
</success_criteria>

<output>
Create `.planning/phases/05-pwa-ify-frontend-mobile-polish/05-06-SUMMARY.md`. Record:
- The exact count of `keyboardType="decimal-pad"` and `inputMode="decimal"` occurrences in TransactionFormScreen.tsx after the edit.
- The three `PASS: <check-name>` lines from the per-check verify-touch-targets invocations (tx-amount-inputmode + login-email-props + register-email-props).
- The aggregate success line `==> all touch-target checks passed` from `--all` (proving the ten-check registry is fully green).
- Confirmation that LoginScreen.tsx and RegisterScreen.tsx were NOT modified by this plan (per RESEARCH § Pitfall 6).
- A copy of the final `verify-pwa.mjs --all` output showing all five PWA checks PASS.
- The `npm test` summary line (must show all suites passing).
</output>
