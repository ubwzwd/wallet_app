---
plan_id: 05-01
phase: 05
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/scripts/verify-pwa.mjs
  - frontend/scripts/verify-touch-targets.mjs
  - frontend/scripts/w3c-manifest-schema.json
  - frontend/__tests__/Button.test.ts
  - frontend/package.json
requirements:
  - PWA-01
  - PWA-02
  - PWA-03
  - PWA-04
  - MOBUI-01
  - MOBUI-02
  - MOBUI-03
  - MOBUI-04
autonomous: true
must_haves:
  truths:
    - "D-14: Running `node frontend/scripts/verify-pwa.mjs --check=manifest` against a valid dist/manifest.json exits 0 — ajv-validated against vendored W3C manifest schema (frontend/scripts/w3c-manifest-schema.json), with per-error FAIL emission."
    - "D-14: Running `node frontend/scripts/verify-touch-targets.mjs --all` against current source (pre-fix) exits non-zero with FAIL output naming every failing check by stable name — the deterministic touch-target static verification check set proves the script can detect regressions across all checks in a single invocation."
    - "D-14: Running `cd frontend && npm test -- --testPathPattern=Button.test.ts` against current Button.tsx (pre-fix) reports failing assertions on minHeight/minWidth — RED state for the MOBUI-01 single-point fix and the Button test component of the D-14 static-check contract."
    - "D-14: `ajv` + vendored W3C manifest schema replace the [ASSUMED] @pwabuilder/manifest-validation route from RESEARCH § Package Legitimacy Audit — ajv is a standard JSON-Schema validator with no legitimacy concern, so no human-verify checkpoint is required."
    - "D-06: `workbox-cli` is installed as a devDependency only (no Workbox runtime imports in app code); everything Workbox-related is generated at build time."
  artifacts:
    - path: "frontend/scripts/verify-pwa.mjs"
      provides: "Static check driver for manifest/apple-meta/viewport/sw/ios-zoom; supports --check=<name> and --all"
    - path: "frontend/scripts/w3c-manifest-schema.json"
      provides: "Vendored W3C Web App Manifest JSON Schema (from SchemaStore) consumed by verify-pwa.mjs --check=manifest via ajv"
    - path: "frontend/scripts/verify-touch-targets.mjs"
      provides: "Source-regex audit with stable check names (button-small-44, screen-safe-area, app-safe-area-provider, tx-amount-inputmode, etc.); supports --check=<name> and --all; aggregates failures in --all mode"
    - path: "frontend/__tests__/Button.test.ts"
      provides: "Jest static-source assertion that Button.tsx `small` style block declares minHeight: 44 and minWidth: 44"
  key_links:
    - from: "frontend/scripts/verify-pwa.mjs"
      to: "frontend/dist/*"
      via: "node:fs.readFileSync"
      pattern: "fs\\.readFileSync.*dist"
    - from: "frontend/scripts/verify-pwa.mjs"
      to: "frontend/scripts/w3c-manifest-schema.json"
      via: "JSON.parse(fs.readFileSync(...)) + ajv.compile(schema)"
      pattern: "w3c-manifest-schema\\.json"
    - from: "frontend/scripts/verify-touch-targets.mjs"
      to: "frontend/src/components/Button.tsx + screens"
      via: "node:fs.readFileSync over src/"
      pattern: "readFileSync.*src/"
---

<objective>
Create the Wave 0 verification harness: two Node scripts (`verify-pwa.mjs`, `verify-touch-targets.mjs`) and one Jest unit test (`Button.test.ts`) that will be referenced by every downstream PLAN's `<verify><automated>` block. Also install the `workbox-cli` and `ajv` devDependencies and vendor the W3C Web App Manifest JSON Schema so the manifest validator has zero runtime network dependency.

Purpose: Without these scripts, every downstream task has no automated verify command and the phase cannot be Nyquist-compliant. Per VALIDATION.md, all PWA-01..04 + MOBUI-01..04 acceptance criteria route through these three artifacts. Both verifier scripts MUST support the same `--check=<name>` / `--all` flag surface so per-task verifies are deterministic and isolated (no cross-check false-positives from exit-on-first-failure). The `--check=manifest` validation uses `ajv` against the vendored W3C schema — real JSON-Schema validation (type checks, enum constraints, conditional required fields), not hand-rolled `if (!manifest.theme_color)` checks. `ajv` is one of the top-30 most-downloaded npm packages (used by ESLint, webpack, AJV-validator ecosystem) — no legitimacy concern, no human-verify checkpoint required.

Output: Two executable Node scripts + one Jest spec + one vendored JSON schema, all committed; `workbox-cli` + `ajv` added to `frontend/package.json` devDependencies; **no production code is touched in this plan** — the scripts must currently FAIL (red state) because the artifacts they verify don't exist yet. That red state is the proof the scripts work.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-pwa-ify-frontend-mobile-polish/05-CONTEXT.md
@.planning/phases/05-pwa-ify-frontend-mobile-polish/05-RESEARCH.md
@.planning/phases/05-pwa-ify-frontend-mobile-polish/05-PATTERNS.md
@.planning/phases/05-pwa-ify-frontend-mobile-polish/05-UI-SPEC.md
@.planning/phases/05-pwa-ify-frontend-mobile-polish/05-VALIDATION.md
@frontend/__tests__/CurrencyPicker.test.ts
@frontend/jest.config.js
@frontend/package.json
@infra/scripts/smoke.sh

<interfaces>
<!-- From frontend/jest.config.js: testEnvironment is 'node' (line 5). All tests MUST be static-source-analysis — no @testing-library/react-native render(). -->
<!-- From frontend/tsconfig.json paths: '@/*' aliases to 'src/*' — but verification scripts under frontend/scripts/*.mjs do NOT use this alias; they use literal path.join(process.cwd(), 'src/...'). -->
<!-- Logging cadence (from infra/scripts/smoke.sh): every step emits `==> step name` to stdout; failures emit `FAIL: reason` to stderr + process.exit(1). No emoji. -->
<!-- Per-task verify command convention (VALIDATION.md): `node frontend/scripts/verify-pwa.mjs --check=<name>` OR `--all`. -->
<!-- verify-touch-targets.mjs mirrors the same surface: `--check=<check-name>` OR `--all` (and `--all` is the default if no flag is given). -->
<!-- ajv API (v8): `import Ajv from 'ajv'; const ajv = new Ajv({ allErrors: true, strict: false }); const validate = ajv.compile(schema); const ok = validate(data); if (!ok) console.error(validate.errors);` -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install workbox-cli + ajv devDependencies and vendor the W3C manifest schema</name>
  <read_first>
    - frontend/package.json (current devDependencies block to extend, not replace)
    - .planning/phases/05-pwa-ify-frontend-mobile-polish/05-RESEARCH.md § Standard Stack (versions verified during research: workbox-cli@^7.4.1) and § Environment Availability (ajv + W3C schema fallback)
    - Schema source: https://json.schemastore.org/web-manifest-combined.json (SchemaStore's curated W3C Web App Manifest schema — the most widely-vendored copy of the W3C manifest spec schema). This is fetched ONCE at install time and committed to the repo as a static asset; verify-pwa.mjs reads it from disk with zero runtime network dependency.
  </read_first>
  <files>frontend/package.json, frontend/package-lock.json, frontend/scripts/w3c-manifest-schema.json</files>
  <action>From `frontend/`, run `npm install --save-dev workbox-cli@^7.4.1 ajv@^8`. Do not modify any other field in package.json. Do NOT add these to `dependencies` — both are dev-only per D-06 and RESEARCH § Don't Hand-Roll. After install, confirm `frontend/package.json` lists both entries under `devDependencies` and that `package-lock.json` was updated.

  Then vendor the W3C Web App Manifest JSON Schema as a static file at `frontend/scripts/w3c-manifest-schema.json`. Fetch it ONCE during this task (e.g. `curl -fsSL https://json.schemastore.org/web-manifest-combined.json -o frontend/scripts/w3c-manifest-schema.json`) and commit the resulting JSON file to the repo. The schema MUST be loaded from disk at runtime by verify-pwa.mjs (no network fetch in the verifier). Verify the downloaded file is valid JSON (`node -e "JSON.parse(require('fs').readFileSync('frontend/scripts/w3c-manifest-schema.json'))"`) and that it contains a `$schema` field plus a `required` or `oneOf` clause referencing core manifest fields (`name`, `start_url`, `display`, etc.).</action>
  <verify>
    <automated>cd frontend && node -e "const p=require('./package.json'); if(!p.devDependencies['workbox-cli']) process.exit(1); if(!p.devDependencies['ajv']) process.exit(1); console.log('==> devDependencies present');" && test -f scripts/w3c-manifest-schema.json && node -e "const s=require('./scripts/w3c-manifest-schema.json'); if(!s['\$schema']) process.exit(1); const blob=JSON.stringify(s); if(!blob.includes('start_url') || !blob.includes('display')) process.exit(1); console.log('==> w3c-manifest-schema.json present and well-formed');"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/package.json` devDependencies block contains `"workbox-cli": "^7.4.1"`.
    - `frontend/package.json` devDependencies block contains `"ajv": "^8"` (or a tighter pin satisfying ^8).
    - Neither package appears under `dependencies`.
    - `frontend/node_modules/workbox-cli/package.json` exists (proves install ran).
    - `frontend/node_modules/ajv/package.json` exists.
    - `frontend/scripts/w3c-manifest-schema.json` exists, is valid JSON, contains a `$schema` field, and references core manifest field names (`name`, `start_url`, `display`).
  </acceptance_criteria>
  <done>Both packages declared as devDependencies, lockfile updated, no entries in `dependencies`, vendored W3C manifest schema committed under `frontend/scripts/`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author frontend/scripts/verify-pwa.mjs static check driver (ajv-validated manifest)</name>
  <read_first>
    - infra/scripts/smoke.sh (lines 30-72 — `==> step` / `FAIL:` / `exit 1` cadence to mirror)
    - frontend/__tests__/CurrencyPicker.test.ts (lines 1-25 — `fs.readFileSync` + path-join idiom)
    - frontend/scripts/w3c-manifest-schema.json (vendored W3C schema authored in Task 1 — loaded via `JSON.parse(fs.readFileSync(...))` and compiled with ajv)
    - .planning/phases/05-pwa-ify-frontend-mobile-polish/05-PATTERNS.md § `frontend/scripts/verify-pwa.mjs` (skeleton + per-check assertion list)
    - .planning/phases/05-pwa-ify-frontend-mobile-polish/05-RESEARCH.md § Validation Architecture § Phase Requirements → Test Map (assertion details for each --check value)
    - .planning/phases/05-pwa-ify-frontend-mobile-polish/05-VALIDATION.md § Per-Task Verification Map (the exact CLI flags downstream plans will invoke)
  </read_first>
  <behavior>
    - Test 1 (--check=manifest, valid manifest): given a `frontend/dist/manifest.json` that has all required fields + ≥1 192x192 icon + ≥1 non-maskable 512x512 + ≥1 maskable icon, ajv validation against the vendored W3C schema returns true and the script exits 0 with `==> manifest:` lines.
    - Test 2 (--check=manifest, missing theme_color): if `theme_color` is removed from the manifest, ajv emits an error (most W3C schema variants list `theme_color` as a recommended-required field; whatever ajv reports gets surfaced). The script exits 1 with a `FAIL: manifest schema violation at <instancePath || '/'>: <message>` line per ajv error entry.
    - Test 3 (--check=manifest, missing maskable icon): if no icon entry has `purpose: "maskable"`, the script supplements ajv with a single hand-rolled check (W3C schema does not enforce purpose=maskable presence) and emits `FAIL: manifest missing maskable icon`. Exits 1.
    - Test 4 (--check=apple-meta): if `dist/index.html` lacks any of `apple-mobile-web-app-capable`, `apple-mobile-web-app-status-bar-style`, `apple-mobile-web-app-title`, `apple-touch-icon`, OR if `dist/apple-touch-icon.png` is absent, script exits 1.
    - Test 5 (--check=viewport): if `dist/index.html` does not contain the literal substring `viewport-fit=cover`, script exits 1.
    - Test 6 (--check=sw): script reads `dist/sw.js`, parses it via `new Function(src)` (catches syntax errors), and asserts the source contains the literal strings `NetworkFirst`, `CacheFirst`, `skipWaiting`, `clientsClaim`, `/api/`, `/index.html`. Any missing string → exit 1.
    - Test 7 (--check=ios-zoom): asserts `frontend/dist/pwa.css` contains the literal substring `font-size: 16px` (the `!important` suffix is verified by a tighter regex `font-size:\s*16px\s*!important`).
    - Test 8 (--all): runs every check above in order; first failure exits 1 immediately.
    - Test 9 (no flag): prints usage to stderr and exits 1.
    - Cadence: every step prints `==> <step name>` to stdout; failures print `FAIL: <reason>` to stderr.
  </behavior>
  <action>Create `frontend/scripts/verify-pwa.mjs` as an ES-module Node script (shebang `#!/usr/bin/env node`). Implement all eight checks listed in `<behavior>`. Use `node:fs`, `node:path`, `node:process`, and import `Ajv` from the `ajv` devDependency (`import Ajv from 'ajv';` — also `import addFormats from 'ajv-formats';` if needed for `uri` / `uri-reference` formats; if ajv complains about unknown format, prefer `new Ajv({ allErrors: true, strict: false })` over installing ajv-formats). Load the W3C schema with `const schema = JSON.parse(fs.readFileSync(path.join(import.meta.dirname || path.dirname(new URL(import.meta.url).pathname), 'w3c-manifest-schema.json'), 'utf8'));` and compile once at module scope (`const validateManifest = new Ajv({ allErrors: true, strict: false }).compile(schema);`).

  In the `--check=manifest` branch: read and parse `dist/manifest.json`, run `const ok = validateManifest(manifest);`. If `!ok`, iterate `validateManifest.errors` and for each error emit one line `FAIL: manifest schema violation at <error.instancePath || '/'>: <error.message>` to stderr, then `process.exit(1)`. Even when ajv passes, run the supplementary hand-rolled check for `purpose: "maskable"` presence (the W3C schema does not enforce it as required) and emit `FAIL: manifest missing maskable icon` + exit 1 if absent.

  Resolve `DIST = path.join(process.cwd(), 'dist')` so the script is invoked from `frontend/`. Parse argv with `process.argv.find(a => a.startsWith('--check='))?.slice(8)` and a `--all` flag. Logging cadence: `==> step name` on stdout, `FAIL: reason` on stderr, `process.exit(1)` on first failure. Make the file executable via `chmod +x` (recorded in git via `git update-index --chmod=+x` if needed — though `node frontend/scripts/verify-pwa.mjs` invocation does not require the +x bit).</action>
  <verify>
    <automated>cd frontend && node scripts/verify-pwa.mjs 2>&1 | grep -q "usage" && node scripts/verify-pwa.mjs --check=manifest 2>&1 | grep -q "FAIL: " && echo "==> verify-pwa.mjs scaffolded and FAILs on missing dist (expected red state)"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/scripts/verify-pwa.mjs` exists with a `#!/usr/bin/env node` shebang on line 1.
    - Source contains the literal strings `import Ajv from 'ajv'`, `w3c-manifest-schema.json`, and `validate.errors` (or `validateManifest.errors` — i.e. the `.errors` accessor used to iterate ajv failures).
    - Source contains the literal strings `--check=manifest`, `--check=apple-meta`, `--check=viewport`, `--check=sw`, `--check=ios-zoom`, `--all`.
    - Source contains the assertion strings `NetworkFirst`, `CacheFirst`, `skipWaiting`, `clientsClaim`, `viewport-fit=cover`, `apple-mobile-web-app-capable`.
    - Source contains the literal failure-prefix `FAIL: manifest schema violation at ` (proves ajv errors are surfaced individually).
    - Running `node frontend/scripts/verify-pwa.mjs --check=manifest` from `frontend/` against the current (no-dist or pre-PWA dist) state exits with code 1 and emits `FAIL:` on stderr (proves the script enforces; not a true negative).
    - Running with no args prints usage and exits 1.
  </acceptance_criteria>
  <done>Script committed; ajv compiles the vendored W3C schema once at module scope; per-error FAIL lines are emitted with `instancePath` + `message`; red-state failure confirmed as the proof of correctness for Wave 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author frontend/scripts/verify-touch-targets.mjs with --check=&lt;name&gt; / --all flag surface</name>
  <read_first>
    - frontend/__tests__/CurrencyPicker.test.ts (file-read + assertion idiom)
    - frontend/src/components/Button.tsx (current small-size style block to assert against)
    - frontend/scripts/verify-pwa.mjs (sibling driver — mirror its argv parsing: `process.argv.find(a => a.startsWith('--check='))?.slice(8)` and the `--all` flag; verify-touch-targets adopts the SAME flag surface so callers can treat both scripts identically)
    - .planning/phases/05-pwa-ify-frontend-mobile-polish/05-PATTERNS.md § `frontend/scripts/verify-touch-targets.mjs` (skeleton, including the regex shapes)
    - .planning/phases/05-pwa-ify-frontend-mobile-polish/05-PATTERNS.md § Touch-Target Spot-Fix table (every file + line + element to be covered by the regex audit)
    - .planning/phases/05-pwa-ify-frontend-mobile-polish/05-VALIDATION.md § Per-Task Verification Map (MOBUI-01, MOBUI-02, MOBUI-04 rows)
  </read_first>
  <behavior>
    - **Argv contract (mirrors verify-pwa.mjs):** The script accepts either `--check=<check-name>` (runs ONLY that check, exits with its single result) OR `--all` (runs every check, aggregates failures). If no flag is given, defaults to `--all` behavior. Unknown `--check=<name>` values print `FAIL: unknown check name: <name>` to stderr and exit 1 (so typos in caller plans are not silent passes).
    - **Failure aggregation in `--all` mode:** Run every check sequentially, but do NOT exit-on-first-failure. Collect all FAILs into an in-memory list. After all checks have run, if the list is non-empty print every collected `FAIL: <check-name> — <reason>` line to stderr and exit 1. If empty, print `==> all touch-target checks passed` to stdout and exit 0. This contract eliminates the false-positive risk where an earlier failing check prevents a later check from running and a downstream `! grep "FAIL: X"` assertion trivially passes.
    - **Single-check mode (`--check=<name>`):** Runs only the named check; if it passes prints `PASS: <name>` to stdout and exits 0; if it fails prints `FAIL: <name> — <reason>` to stderr and exits 1.
    - **Stable check-name registry + emit-string contract (locked — downstream plans grep these):**

      | check-name                  | one-line description (used in `==>` entry line)                                                                  | Target file(s) + assertion                                                                                              |
      |-----------------------------|-------------------------------------------------------------------------------------------------------------------|--------------------------------------------------------------------------------------------------------------------------|
      | `button-small-44`           | Button.tsx `small` size declares minHeight: 44 and minWidth: 44                                                  | `src/components/Button.tsx` regex `/small:\s*\{[^}]*minHeight:\s*44[^}]*minWidth:\s*44/s` (either property order OK)     |
      | `finance-form-spot-fix-44`  | FinanceSourceFormScreen.tsx contains ≥1 `minHeight: 44`                                                          | `src/screens/FinanceSourceFormScreen.tsx` substring `minHeight: 44`                                                       |
      | `tx-form-spot-fix-44`       | TransactionFormScreen.tsx contains ≥1 `minHeight: 44`                                                            | `src/screens/TransactionFormScreen.tsx` substring `minHeight: 44`                                                         |
      | `profile-spot-fix-44`       | ProfileScreen.tsx contains ≥1 `minHeight: 44`                                                                    | `src/screens/ProfileScreen.tsx` substring `minHeight: 44`                                                                 |
      | `tx-amount-inputmode`       | TransactionFormScreen.tsx inputMode=decimal count matches keyboardType=decimal-pad count and both ≥3             | `src/screens/TransactionFormScreen.tsx` paired counts of `inputMode="decimal"` and `keyboardType="decimal-pad"`           |
      | `login-email-props`         | LoginScreen.tsx declares keyboardType=email-address and autoComplete=email                                       | `src/screens/LoginScreen.tsx` both substrings present                                                                     |
      | `register-email-props`      | RegisterScreen.tsx declares keyboardType=email-address and autoComplete=email                                    | `src/screens/RegisterScreen.tsx` both substrings present                                                                  |
      | `screen-safe-area`          | Screen.tsx imports SafeAreaView from react-native-safe-area-context (and not from react-native)                  | `src/components/Screen.tsx` regex `/from\s+['"]react-native-safe-area-context['"]/` matches AND `SafeAreaView` is not destructured from `'react-native'` |
      | `app-safe-area-provider`    | App.tsx wraps the tree with SafeAreaProvider imported from react-native-safe-area-context                        | `App.tsx` contains literal `SafeAreaProvider` AND imports from `'react-native-safe-area-context'`                         |

    - **Emit-string contract (locked — these are the exact strings downstream plans grep):**
      - On entry (before running a single check): `==> verify-touch-targets[<check-name>]: <one-line description from the table>` to stdout.
      - On pass: `PASS: <check-name>` to stdout.
      - On fail: `FAIL: <check-name> — <reason>` to stderr. The reason MUST follow the em-dash separator and be a concrete diagnostic (e.g., `FAIL: button-small-44 — Button.tsx small size missing minHeight: 44`).
      - In `--all` mode, the aggregated failure list at the end is just every collected `FAIL: <check-name> — <reason>` line printed in the order checks were run.
    - Cadence: same `==> step name` / `FAIL: reason` shape as verify-pwa.mjs; readSrc helper invoked from `frontend/`.
  </behavior>
  <action>Create `frontend/scripts/verify-touch-targets.mjs` as an ES-module Node script. Implement the nine checks in the registry per `<behavior>`. Use only `node:fs`, `node:path`. Define `readSrc(rel) { return fs.readFileSync(path.join(process.cwd(), rel), 'utf8'); }`.

  Argv parsing: `const checkArg = process.argv.find(a => a.startsWith('--check='))?.slice(8); const allMode = process.argv.includes('--all') || !checkArg;`. If `checkArg` is set, look it up in a registry object `{ 'button-small-44': fn, 'finance-form-spot-fix-44': fn, ... }`; unknown name → `console.error('FAIL: unknown check name: ' + checkArg); process.exit(1);`.

  Each check is a function that returns `{ ok: boolean, reason?: string }`. The driver emits the `==>` entry line BEFORE calling the function, then emits either `PASS: <name>` or `FAIL: <name> — <reason>` based on the return. In `--all` mode, never `process.exit(1)` mid-loop; collect failures in `const fails = []` and exit at the end based on `fails.length`.

  Last line of stdout on `--all` success: `==> all touch-target checks passed`.</action>
  <verify>
    <automated>cd frontend && (node scripts/verify-touch-targets.mjs --all; rc=$?; test $rc -ne 0) && node scripts/verify-touch-targets.mjs --all 2>&1 | grep -qE "^==> verify-touch-targets\[button-small-44\]:" && node scripts/verify-touch-targets.mjs --all 2>&1 | grep -qE "^FAIL: button-small-44 — " && (node scripts/verify-touch-targets.mjs --check=unknown-check-foo; rc=$?; test $rc -ne 0) && echo "==> verify-touch-targets.mjs scaffolded; --all aggregates failures; --check=&lt;name&gt; isolates; unknown name fails loud"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/scripts/verify-touch-targets.mjs` exists with a `#!/usr/bin/env node` shebang.
    - Source contains the literal stable check-name strings: `button-small-44`, `finance-form-spot-fix-44`, `tx-form-spot-fix-44`, `profile-spot-fix-44`, `tx-amount-inputmode`, `login-email-props`, `register-email-props`, `screen-safe-area`, `app-safe-area-provider`.
    - Source contains the literal emit-string prefixes `==> verify-touch-targets[`, `PASS: `, and `FAIL: ` (with the em-dash `— ` separator for FAIL reasons).
    - Source contains the argv-parsing literals `--check=` and `--all`.
    - Source contains the regex literal `small:\\s*\\{[^}]*minHeight:\\s*44` (or equivalent JS-escaped form).
    - Source contains literal strings `react-native-safe-area-context`, `SafeAreaProvider`, `inputMode="decimal"`, `autoComplete="email"`.
    - Running `node frontend/scripts/verify-touch-targets.mjs --all` from `frontend/` against current (pre-fix) source exits with code 1 AND emits at least one `==> verify-touch-targets[<check-name>]:` entry line for EVERY check in the registry (proving --all does not exit on first failure) AND emits aggregated `FAIL: <check-name> — <reason>` lines for the pre-fix red checks (at minimum `button-small-44`, `tx-amount-inputmode`, `screen-safe-area`, `app-safe-area-provider`).
    - Running `node frontend/scripts/verify-touch-targets.mjs --check=button-small-44` exits with code 1 against pre-fix source AND emits exactly one `==> verify-touch-targets[button-small-44]:` line followed by one `FAIL: button-small-44 — <reason>` line (no other check-name strings in output).
    - Running `node frontend/scripts/verify-touch-targets.mjs --check=login-email-props` exits with code 0 against current (already-compliant) source AND emits `PASS: login-email-props`.
    - Running `node frontend/scripts/verify-touch-targets.mjs --check=unknown-check-foo` exits with code 1 AND emits `FAIL: unknown check name: unknown-check-foo` (no silent pass on typos).
  </acceptance_criteria>
  <done>Script committed; `--all` aggregates failures across all nine checks; `--check=<name>` isolates a single check; unknown check names fail loud; the locked emit-string contract (`==> verify-touch-targets[<name>]:`, `PASS: <name>`, `FAIL: <name> — <reason>`) holds for every check in the registry.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Author frontend/__tests__/Button.test.ts static-source assertion for MOBUI-01</name>
  <read_first>
    - frontend/__tests__/CurrencyPicker.test.ts (exact shape — `fs.readFileSync` + describe/it/expect, testEnvironment: 'node')
    - frontend/__tests__/TransactionsScreen.test.ts (additional describe/it idioms with regex assertions)
    - frontend/src/components/Button.tsx (the file under test; the `small` size block currently lacks minHeight/minWidth — test MUST fail until 05-04 fixes it)
    - frontend/jest.config.js (confirm `testEnvironment: 'node'` — no RN render allowed)
    - .planning/phases/05-pwa-ify-frontend-mobile-polish/05-PATTERNS.md § `frontend/__tests__/Button.test.ts` (idioms + warning against `@testing-library/react-native`)
  </read_first>
  <behavior>
    - Top-level describe: "MOBUI-01: Button small size satisfies 44x44 touch-target minimum".
    - Three `it` blocks:
      1. "Button.tsx defines a small size style block" — `expect(BUTTON_SOURCE).toMatch(/small:\s*\{/)`.
      2. "small size has minHeight: 44" — `expect(BUTTON_SOURCE).toMatch(/small:\s*\{[^}]*minHeight:\s*44/s)`.
      3. "small size has minWidth: 44" — `expect(BUTTON_SOURCE).toMatch(/small:\s*\{[^}]*minWidth:\s*44/s)`.
    - First `it` passes against the current Button.tsx (the small block already exists). Second and third `it` FAIL until plan 05-04 lands its edit. That red state is intentional.
  </behavior>
  <action>Create `frontend/__tests__/Button.test.ts`. Use `import * as fs from 'fs'; import * as path from 'path';` exactly as `CurrencyPicker.test.ts` does. Read the Button source via `fs.readFileSync(path.join(__dirname, '../src/components/Button.tsx'), 'utf8')` into a top-level constant `BUTTON_SOURCE`. Implement the three `it` blocks per `<behavior>`. Do NOT import anything from `'react-native'`, do NOT call `render()` or use `@testing-library/react-native` — `jest.config.js` sets `testEnvironment: 'node'` (line 5) precisely to forbid the RN runtime.</action>
  <verify>
    <automated>cd frontend && (npm test -- --testPathPattern=Button.test.ts 2>&1 | tee /tmp/btn-test.log; rc=${PIPESTATUS[0]}; test $rc -ne 0) && grep -q "minHeight: 44" /tmp/btn-test.log && echo "==> Button.test.ts present and failing on minHeight (expected red state)"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/__tests__/Button.test.ts` exists.
    - Source contains `describe('MOBUI-01: Button small size satisfies 44x44 touch-target minimum'` (substring match).
    - Source contains the three regex literals `/small:\\s*\\{/`, `minHeight:\\s*44`, `minWidth:\\s*44` (in JS-source-escaped form).
    - Source does NOT contain `from 'react-native'` or `'@testing-library`.
    - `cd frontend && npm test -- --testPathPattern=Button.test.ts` exits non-zero AND the output mentions a failing assertion containing `minHeight` (the small block doesn't have it yet — by design).
  </acceptance_criteria>
  <done>Spec committed; npm test shows 1 pass + 2 fail (Wave 0 red state confirmed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local dev shell ↔ npm registry | `npm install` fetches third-party JS into node_modules; postinstall scripts run with developer-shell privileges |
| Verification script ↔ filesystem | Scripts read `dist/*` and `src/*`; no network, no writes outside its own stderr — minimal blast radius |
| One-time schema fetch ↔ schemastore.org | Task 1 fetches the W3C manifest schema ONCE at install time and commits the JSON to the repo; verify-pwa.mjs has zero runtime network dependency thereafter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-SC | Tampering | `npm install workbox-cli + ajv` (supply-chain) | mitigate | Both packages are widely-deployed standards (workbox-cli per RESEARCH § Package Legitimacy Audit [OK]; ajv is one of the top-30 most-downloaded npm packages, used by ESLint, webpack, and the AJV-validator ecosystem — slopcheck-equivalent). No human-verify checkpoint required for ajv. |
| T-05-01-02 | Tampering / data freshness | n/a in this plan — no SW work happens here | accept | Stale-shell trap is mitigated in plan 05-03 (Workbox config + registerSW). Wave 0 only authors verifiers. |
| T-05-01-03 | Information Disclosure | Cross-user cache leakage on shared devices | accept (out of scope) | Phase scope is verifiers only; the `caches.delete('api-cache')` helper is documented in RESEARCH § Security Domain as a future ticket. Not added to Phase 5. |
| T-05-01-04 | Tampering / Spoofing | Service worker hijack | accept | SW does not exist in this plan; mitigated structurally by HTTPS from Phase 4 once the SW lands in 05-03. |
| T-05-01-05 | Tampering | Cache poisoning via long-lived CacheFirst | accept | No SW in this plan; mitigation lives in 05-03 via `cleanupOutdatedCaches: true` + Expo content-hashed filenames. |
| T-05-01-06 | Tampering | Reflected XSS via manifest fields | accept | No manifest in this plan; manifest authoring is in 05-02 (static authored file — non-issue). |
| T-05-01-SCHEMA | Tampering | One-time fetch of W3C manifest schema from schemastore.org | accept | Schema is fetched ONCE during Task 1 and committed to the repo as a static asset; thereafter verify-pwa.mjs reads it from disk with zero runtime network dependency. Schema content is human-reviewable JSON (no executable code). |
</threat_model>

<verification>
After all tasks in this plan complete:

```bash
# Both new scripts exist, are syntactically valid Node, and FAIL against the pre-fix codebase
cd frontend
node scripts/verify-pwa.mjs 2>&1 | grep -E "usage|FAIL" >/dev/null
node scripts/verify-touch-targets.mjs --all 2>&1 | grep -E "FAIL: " >/dev/null
node scripts/verify-touch-targets.mjs --check=button-small-44 2>&1 | grep -E "^FAIL: button-small-44 — " >/dev/null
node scripts/verify-touch-targets.mjs --check=login-email-props 2>&1 | grep -E "^PASS: login-email-props" >/dev/null

# Button.test.ts is present and red
npm test -- --testPathPattern=Button.test.ts 2>&1 | grep -E "FAIL|fail" >/dev/null

# Both devDeps registered + W3C schema vendored
node -e "const p=require('./package.json'); console.log(p.devDependencies['workbox-cli'], p.devDependencies['ajv'])" | grep -v undefined >/dev/null
test -f scripts/w3c-manifest-schema.json
```

All six assertions must succeed for the plan to be complete.
</verification>

<success_criteria>
1. `frontend/scripts/verify-pwa.mjs` exists with the 5-check + `--all` driver, ajv-validates the manifest against the vendored W3C schema, and exits 1 against pre-PWA source.
2. `frontend/scripts/verify-touch-targets.mjs` exists with the nine-check registry, supports both `--check=<name>` and `--all`, aggregates failures in `--all` mode (does NOT exit-on-first-failure), and emits the locked `==> verify-touch-targets[<name>]:` / `PASS: <name>` / `FAIL: <name> — <reason>` strings.
3. `frontend/__tests__/Button.test.ts` exists; `npm test` shows 1 pass + 2 fail.
4. `workbox-cli@^7.4.1` + `ajv@^8` declared under `devDependencies` only.
5. `frontend/scripts/w3c-manifest-schema.json` exists, is valid JSON, and contains the W3C Web App Manifest schema referenced by verify-pwa.mjs.
</success_criteria>

<output>
Create `.planning/phases/05-pwa-ify-frontend-mobile-polish/05-01-SUMMARY.md` on completion. Summary must record:
- That `ajv@^8` is declared under `devDependencies` and that the vendored W3C manifest schema lives at `frontend/scripts/w3c-manifest-schema.json` (with its source URL for reproducibility).
- The complete list of stable check-names registered in verify-touch-targets.mjs (so downstream plans can grep the SUMMARY for the contract).
- The exact list of FAILing assertions observed against the pre-fix code (with their `<check-name>` identifiers) so downstream plans know which red checks must turn green.
- Confirmation that no production source (anything under `frontend/src/`) was modified by this plan.
</output>
</output>
</output>
