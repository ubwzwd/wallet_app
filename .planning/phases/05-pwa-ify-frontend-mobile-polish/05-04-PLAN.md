---
plan_id: 05-04
phase: 05
plan: 04
type: execute
wave: 1
depends_on: ["05-01"]
files_modified:
  - frontend/src/components/Button.tsx
  - frontend/src/screens/FinanceSourceFormScreen.tsx
  - frontend/src/screens/TransactionFormScreen.tsx
  - frontend/src/screens/ProfileScreen.tsx
requirements:
  - MOBUI-01
autonomous: true
must_haves:
  truths:
    - "Every `<Button size='small'>` call-site across the app (14 sites confirmed by PATTERNS.md grep) inherits a 44×44 minimum touch target after the single-point edit to Button.tsx, without per-call-site changes."
    - "Non-Button touch-critical sites named in UI-SPEC Touch Target Enforcement Contract (currency pickers, modal close buttons, back button) explicitly declare `minHeight: 44` in their styles."
    - "Running `cd frontend && npm test -- --testPathPattern=Button.test.ts` reports 3/3 tests passing (GREEN state for MOBUI-01) — the Wave-0 red baseline from 05-01 Task 5 flips."
    - "Running `node frontend/scripts/verify-touch-targets.mjs` Checks A and B both PASS after this plan."
  artifacts:
    - path: "frontend/src/components/Button.tsx"
      provides: "Button `small` size style declares minHeight: 44 and minWidth: 44 — propagates to all 14 small-button call-sites"
      contains: "minHeight: 44"
    - path: "frontend/src/screens/FinanceSourceFormScreen.tsx"
      provides: "currencyTrigger + modal close + currency-row styles each declare minHeight: 44 (modal close also minWidth: 44)"
      contains: "minHeight: 44"
    - path: "frontend/src/screens/TransactionFormScreen.tsx"
      provides: "Same three touch-critical sites (currencyTrigger / modal close / currency row) — same fix"
      contains: "minHeight: 44"
    - path: "frontend/src/screens/ProfileScreen.tsx"
      provides: "backButton (44×44), currencyTrigger (minHeight:44), modal close (44×44)"
      contains: "minHeight: 44"
  key_links:
    - from: "Every <Button size='small'> call-site"
      to: "frontend/src/components/Button.tsx styles.small"
      via: "StyleSheet inheritance"
      pattern: "size=\"small\""
    - from: "Currency-picker TouchableOpacities (3 screens × 3 sites)"
      to: "explicit minHeight/minWidth declarations"
      via: "per-site style edit"
      pattern: "minHeight: 44"
---

<objective>
Land the MOBUI-01 fix: the **Button.tsx single-point edit** that propagates to every `<Button size="small">` call-site across the app, PLUS the named non-Button touch-critical sites in UI-SPEC's Touch Target Enforcement Contract (currency pickers, modal close buttons, back buttons in ProfileScreen / FinanceSourceFormScreen / TransactionFormScreen).

Purpose: Per CONTEXT D-15, this is NOT a full grep audit of every TouchableOpacity — UI-SPEC names the touch-critical sites and the planner trusts that list. Per PATTERNS.md the Button.tsx edit is the highest-leverage change in the phase (one edit → 14 inherited fixes across 5 screens). The non-Button spot-fixes target six screen-level TouchableOpacities that don't go through `<Button>`.

Output: One single-point edit to `Button.tsx`; targeted style additions in three screen files; the `Button.test.ts` red baseline from 05-01 Task 5 flips GREEN; `verify-touch-targets.mjs` Checks A and B both turn GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-pwa-ify-frontend-mobile-polish/05-CONTEXT.md
@.planning/phases/05-pwa-ify-frontend-mobile-polish/05-UI-SPEC.md
@.planning/phases/05-pwa-ify-frontend-mobile-polish/05-RESEARCH.md
@.planning/phases/05-pwa-ify-frontend-mobile-polish/05-PATTERNS.md
@.planning/phases/05-pwa-ify-frontend-mobile-polish/05-01-SUMMARY.md
@frontend/src/components/Button.tsx
@frontend/src/screens/FinanceSourceFormScreen.tsx
@frontend/src/screens/TransactionFormScreen.tsx
@frontend/src/screens/ProfileScreen.tsx
@frontend/src/screens/TransactionsScreen.tsx
@frontend/src/screens/FinanceSourcesScreen.tsx

<interfaces>
<!-- Button.tsx `small` size block (PATTERNS § Button.tsx diff, target shape): -->
<!--   small: { -->
<!--     paddingVertical: 8, -->
<!--     paddingHorizontal: 16, -->
<!--     minHeight: 44,   <-- ADD -->
<!--     minWidth: 44,    <-- ADD -->
<!--   }, -->

<!-- Touch-Target Spot-Fix Table (PATTERNS § Shared Patterns § Touch-Target Spot-Fix — locked list, do NOT grep-audit beyond it): -->
<!--   File                                 | Element             | Fix -->
<!--   Button.tsx                           | small size style    | minHeight: 44 + minWidth: 44 -->
<!--   FinanceSourceFormScreen.tsx          | currencyTrigger     | minHeight: 44 -->
<!--   FinanceSourceFormScreen.tsx          | modal close         | minHeight: 44 + minWidth: 44 + padding: 12 + justifyContent: 'center' -->
<!--   FinanceSourceFormScreen.tsx          | currency-row item   | minHeight: 44 -->
<!--   TransactionFormScreen.tsx            | currencyTrigger     | minHeight: 44 -->
<!--   TransactionFormScreen.tsx            | modal close         | minHeight: 44 + minWidth: 44 + padding: 12 -->
<!--   TransactionFormScreen.tsx            | currency-row item   | minHeight: 44 -->
<!--   ProfileScreen.tsx                    | backButton          | minHeight: 44 + minWidth: 44 -->
<!--   ProfileScreen.tsx                    | currencyTrigger     | minHeight: 44 -->
<!--   ProfileScreen.tsx                    | modal close         | minHeight: 44 + minWidth: 44 + padding: 12 -->

<!-- TransactionsScreen.tsx + FinanceSourcesScreen.tsx: -->
<!--   No per-site edits — every interactive element uses <Button size="small"> (PATTERNS confirms by grep). -->
<!--   Inheritance from the Button.tsx single-point fix is the gate; verify-touch-targets does not regex these files. -->

<!-- Per CONTEXT D-15 + RESEARCH § Pitfall 7: tag chips are display-only (no onPress) — MOBUI-01 compliance by-definition; no edit. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apply the Button.tsx single-point fix (minHeight: 44 + minWidth: 44 on `small` size)</name>
  <read_first>
    - frontend/src/components/Button.tsx (full file — locate the `small:` style block in the StyleSheet.create call near the bottom)
    - .planning/phases/05-pwa-ify-frontend-mobile-polish/05-PATTERNS.md § Button.tsx (the locked diff)
    - .planning/phases/05-pwa-ify-frontend-mobile-polish/05-UI-SPEC.md § Touch Target Enforcement Contract (the "single-point fix" rationale)
    - frontend/__tests__/Button.test.ts (from 05-01 Task 5 — the three RED assertions this edit will flip GREEN)
  </read_first>
  <behavior>
    - After edit, `frontend/src/components/Button.tsx` `small` size style block contains both `minHeight: 44` and `minWidth: 44` declarations alongside the existing `paddingVertical: 8` and `paddingHorizontal: 16`.
    - All other style blocks (`medium`, `large`, variants `primary`/`secondary`/`destructive`, text styles) remain byte-identical.
    - No other file is modified in this task.
    - `cd frontend && npm test -- --testPathPattern=Button.test.ts` reports 3/3 tests passing (the test from 05-01 Task 5 was 1 pass + 2 fail; after this edit it must be all 3 GREEN).
    - `verify-touch-targets.mjs` Check A (Button.tsx) PASSES.
  </behavior>
  <files>frontend/src/components/Button.tsx</files>
  <action>
    Open `frontend/src/components/Button.tsx`. Locate the StyleSheet.create block (near the bottom of the file, around lines 69-137 per PATTERNS.md). Inside, find the `small: { ... }` style block. The block currently contains exactly two properties: `paddingVertical: 8` and `paddingHorizontal: 16`. Add two new properties: `minHeight: 44` and `minWidth: 44`. The order doesn't matter; the result is a block with four properties. Match the existing indentation style (4 spaces under the `small:` key).

    Do NOT modify any other style block. Do NOT modify the component function body. Do NOT modify imports. Do NOT change the existing `medium` and `large` blocks (those already produce ≥44px touch targets via their padding alone — verify by reading the file, but do not edit defensively).
  </action>
  <verify>
    <automated>cd frontend && node -e "const s=require('fs').readFileSync('src/components/Button.tsx','utf8'); if(!/small:\s*\{[^}]*minHeight:\s*44/s.test(s))throw new Error('minHeight: 44 missing in small block'); if(!/small:\s*\{[^}]*minWidth:\s*44/s.test(s))throw new Error('minWidth: 44 missing in small block'); console.log('==> Button.tsx small block has minHeight 44 + minWidth 44');" && npm test -- --testPathPattern=Button.test.ts 2>&1 | grep -E "(Tests:.*passed|3 passed)" && echo "==> Button.test.ts now 3/3 GREEN"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/Button.tsx` regex `/small:\s*\{[^}]*minHeight:\s*44/s` matches.
    - `frontend/src/components/Button.tsx` regex `/small:\s*\{[^}]*minWidth:\s*44/s` matches.
    - `cd frontend && npm test -- --testPathPattern=Button.test.ts` exits 0 AND reports 3 passing tests (no failures).
    - `git diff src/components/Button.tsx` shows changes ONLY in the small-size style block — no edits to component body or other style blocks.
  </acceptance_criteria>
  <done>Button.tsx small-size style declares both 44s; Button.test.ts flips GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Touch-target spot-fixes in FinanceSourceFormScreen.tsx (currencyTrigger + modal close + currency-row)</name>
  <read_first>
    - frontend/src/screens/FinanceSourceFormScreen.tsx (full file — locate the three sites: `styles.currencyTrigger` definition, the modal-close `<TouchableOpacity onPress={() => setCurrencyPickerVisible(false)}>` element, and `styles.currencyItem` definition; PATTERNS guidance ~lines 175/191/204 may have drifted)
    - .planning/phases/05-pwa-ify-frontend-mobile-polish/05-PATTERNS.md § FinanceSourceFormScreen.tsx (the three locked edits)
    - .planning/phases/05-pwa-ify-frontend-mobile-polish/05-UI-SPEC.md § Touch Target Enforcement Contract
  </read_first>
  <behavior>
    - After edit, `styles.currencyTrigger` (a StyleSheet entry) declares `minHeight: 44`.
    - After edit, `styles.currencyItem` (the currency-row entry) declares `minHeight: 44`.
    - The modal close TouchableOpacity has inline `style={{ minHeight: 44, minWidth: 44, padding: 12, justifyContent: 'center' }}` OR a referenced style object with those exact four properties.
    - No other style block in the file changes.
    - File still parses + tests still pass (`cd frontend && npm test` reports zero failing suites).
  </behavior>
  <files>frontend/src/screens/FinanceSourceFormScreen.tsx</files>
  <action>
    Open `frontend/src/screens/FinanceSourceFormScreen.tsx`. Make three targeted edits:

    1. In the StyleSheet.create block, find the `currencyTrigger` style. Add `minHeight: 44` as a sibling property (alongside whatever padding/border properties already exist). Do NOT remove or modify the existing properties.

    2. Find the StyleSheet `currencyItem` style (the currency-row item). Add `minHeight: 44` as a sibling property.

    3. Find the modal-close TouchableOpacity (the one whose `onPress` calls `setCurrencyPickerVisible(false)` or similar). It currently has no inline minHeight/minWidth. Either (a) add an inline `style={{ minHeight: 44, minWidth: 44, padding: 12, justifyContent: 'center' }}` to the element, OR (b) create a new `modalCloseButton` style in the StyleSheet with those four properties and apply it via `style={styles.modalCloseButton}`. Choose (a) if the executor prefers to minimize the StyleSheet surface; choose (b) if there's a pattern elsewhere in the file that uses named close-button styles. Either is acceptable provided the four property values are present.

    Do NOT touch any other TouchableOpacity in the file (the Save/Cancel `<Button>` components inherit the Button.tsx single-point fix from Task 1; no per-site edit needed).
  </action>
  <verify>
    <automated>cd frontend && node -e "const s=require('fs').readFileSync('src/screens/FinanceSourceFormScreen.tsx','utf8'); const m44=(s.match(/minHeight:\s*44/g)||[]).length; if(m44<3)throw new Error('expected ≥3 occurrences of minHeight: 44 in FinanceSourceFormScreen.tsx, got '+m44); const mw44=(s.match(/minWidth:\s*44/g)||[]).length; if(mw44<1)throw new Error('expected ≥1 occurrence of minWidth: 44 (modal close), got '+mw44); console.log('==> FinanceSourceFormScreen.tsx has '+m44+' minHeight:44 and '+mw44+' minWidth:44');" && npm test -- --testPathPattern=FinanceSourcesScreen 2>&1 | grep -E "(passed|0 failed)" && echo "==> FinanceSourcesScreen.test.ts still passing"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/screens/FinanceSourceFormScreen.tsx` contains at least 3 occurrences of `minHeight: 44` (one each for currencyTrigger, currencyItem, modal close).
    - `frontend/src/screens/FinanceSourceFormScreen.tsx` contains at least 1 occurrence of `minWidth: 44` (the modal close).
    - The existing `FinanceSourcesScreen.test.ts` static-analysis spec still passes (unchanged regression behavior).
    - `git diff src/screens/FinanceSourceFormScreen.tsx` shows only additions of style properties — no logic changes, no import changes, no validation changes.
  </acceptance_criteria>
  <done>Three touch-critical sites in FinanceSourceFormScreen.tsx satisfy 44×44; existing tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Touch-target spot-fixes in TransactionFormScreen.tsx (currencyTrigger + modal close + currency-row)</name>
  <read_first>
    - frontend/src/screens/TransactionFormScreen.tsx (full file — locate the same three site categories as Task 2; PATTERNS guidance ~lines 542/456/466 may have drifted)
    - .planning/phases/05-pwa-ify-frontend-mobile-polish/05-PATTERNS.md § TransactionFormScreen.tsx ("Also-modify" line + the currency picker block details)
    - .planning/phases/05-pwa-ify-frontend-mobile-polish/05-UI-SPEC.md § Touch Target Enforcement Contract
  </read_first>
  <behavior>
    - Same shape as Task 2: three minHeight: 44 additions (currencyTrigger, currencyItem, modal close) + one minWidth: 44 (modal close).
    - No edits to amount-field TextInputs (those are owned by plan 05-06).
    - Existing tests still pass.
  </behavior>
  <files>frontend/src/screens/TransactionFormScreen.tsx</files>
  <action>
    Open `frontend/src/screens/TransactionFormScreen.tsx`. Make the same three edits as Task 2 against this file:

    1. Add `minHeight: 44` to the `currencyTrigger` style in the StyleSheet.create block.
    2. Add `minHeight: 44` to the `currencyItem` (currency-row) style.
    3. On the modal-close TouchableOpacity (the one inside the currency picker modal that closes it), add inline `style={{ minHeight: 44, minWidth: 44, padding: 12 }}` OR add a named `modalCloseButton` style with those properties applied via `style={styles.modalCloseButton}`.

    DO NOT modify the amount-field TextInputs — those are exclusively edited by plan 05-06 (the inputMode="decimal" prop). To preserve the parallel-execution contract (this plan and 05-06 may run concurrently in Wave 1), this task touches ONLY the currency-picker block and never the amount-field block.
  </action>
  <verify>
    <automated>cd frontend && node -e "const s=require('fs').readFileSync('src/screens/TransactionFormScreen.tsx','utf8'); const m44=(s.match(/minHeight:\s*44/g)||[]).length; if(m44<3)throw new Error('expected ≥3 occurrences of minHeight: 44 in TransactionFormScreen.tsx, got '+m44); const mw44=(s.match(/minWidth:\s*44/g)||[]).length; if(mw44<1)throw new Error('expected ≥1 occurrence of minWidth: 44 (modal close), got '+mw44); console.log('==> TransactionFormScreen.tsx has '+m44+' minHeight:44 and '+mw44+' minWidth:44');" && ! grep -F 'inputMode="decimal"' src/screens/TransactionFormScreen.tsx 2>/dev/null || echo "==> inputMode edits NOT touched (owned by 05-06)" && echo "==> TransactionFormScreen touch-target edits applied scoped to currency block"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/screens/TransactionFormScreen.tsx` contains at least 3 occurrences of `minHeight: 44`.
    - At least 1 occurrence of `minWidth: 44`.
    - `git diff` shows changes ONLY in the currency picker block (currencyTrigger + currencyItem styles + modal close TouchableOpacity) — no edits to amount-field TextInputs, no edits to validation hooks.
    - `cd frontend && npm test` reports zero failing suites.
  </acceptance_criteria>
  <done>TransactionFormScreen's currency-picker block satisfies 44×44 touch-target contract; amount-field edits intentionally untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Touch-target spot-fixes in ProfileScreen.tsx (backButton + currencyTrigger + modal close)</name>
  <read_first>
    - frontend/src/screens/ProfileScreen.tsx (full file — locate the back button at ~line 98, currency trigger at ~122, modal close at ~164 per PATTERNS guidance)
    - .planning/phases/05-pwa-ify-frontend-mobile-polish/05-PATTERNS.md § ProfileScreen.tsx (the three locked edits)
    - .planning/phases/05-pwa-ify-frontend-mobile-polish/05-UI-SPEC.md § Touch Target Enforcement Contract (back buttons + modal close)
  </read_first>
  <behavior>
    - `styles.backButton` (or equivalent) declares both `minHeight: 44` and `minWidth: 44`.
    - `styles.currencyTrigger` declares `minHeight: 44`.
    - Modal close TouchableOpacity declares `minHeight: 44 + minWidth: 44 + padding: 12` (inline or named style).
    - The Save/Cancel `<Button>` components on ProfileScreen are NOT touched (they inherit from Button.tsx Task 1).
  </behavior>
  <files>frontend/src/screens/ProfileScreen.tsx</files>
  <action>
    Open `frontend/src/screens/ProfileScreen.tsx`. Make three edits:

    1. Find `styles.backButton` (the style for the back/header button TouchableOpacity, around line 98 per PATTERNS). Add both `minHeight: 44` AND `minWidth: 44` properties.

    2. Find `styles.currencyTrigger`. Add `minHeight: 44`.

    3. Find the modal-close TouchableOpacity (around line 164 per PATTERNS — the one whose `onPress` calls `setPickerVisible(false)` or equivalent). Add inline `style={{ minHeight: 44, minWidth: 44, padding: 12 }}` OR a named style with those three properties.

    Do NOT touch the `<Button>` Save/Cancel components (lines ~135, ~142 per PATTERNS) — they inherit the Button.tsx single-point fix automatically.
  </action>
  <verify>
    <automated>cd frontend && node -e "const s=require('fs').readFileSync('src/screens/ProfileScreen.tsx','utf8'); const m44=(s.match(/minHeight:\s*44/g)||[]).length; if(m44<3)throw new Error('expected ≥3 occurrences of minHeight: 44, got '+m44); const mw44=(s.match(/minWidth:\s*44/g)||[]).length; if(mw44<2)throw new Error('expected ≥2 occurrences of minWidth: 44 (backButton + modal close), got '+mw44); console.log('==> ProfileScreen.tsx has '+m44+' minHeight:44 and '+mw44+' minWidth:44');"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/screens/ProfileScreen.tsx` contains at least 3 occurrences of `minHeight: 44` (backButton + currencyTrigger + modal close).
    - At least 2 occurrences of `minWidth: 44` (backButton + modal close).
    - `git diff` shows only style additions; no JSX restructuring, no import changes.
  </acceptance_criteria>
  <done>ProfileScreen's three touch-critical sites satisfy the 44×44 contract.</done>
</task>

<task type="auto">
  <name>Task 5: Run verify-touch-targets.mjs Checks A + B and confirm both green</name>
  <read_first>
    - frontend/scripts/verify-touch-targets.mjs (from 05-01 Task 4 — Checks A and B target Button.tsx + the three screen files)
    - .planning/phases/05-pwa-ify-frontend-mobile-polish/05-VALIDATION.md § Per-Task Verification Map (MOBUI-01)
  </read_first>
  <files>(no edits; verification gate)</files>
  <action>
    From `frontend/`, run `node scripts/verify-touch-targets.mjs`. Inspect the output. At this point in Wave 1:
    - Check A (Button.tsx small size minHeight + minWidth) MUST PASS (Task 1 landed it).
    - Check B (per-file minHeight: 44 in FinanceSourceFormScreen, TransactionFormScreen, ProfileScreen) MUST PASS (Tasks 2-4 landed them).
    - Checks C (TransactionFormScreen inputMode) — MAY still FAIL (owned by 05-06).
    - Checks D (LoginScreen + RegisterScreen email props) — PASS (pre-existing compliance).
    - Checks E + F (Screen.tsx + App.tsx safe-area) — MAY still FAIL (owned by 05-05).

    The acceptance criterion for this task is specifically Checks A and B GREEN; failures on C, E, F are expected if their owning plans haven't merged yet. If Check A or B FAIL, return to Task 1 (regex-mismatched small block) or Tasks 2-4 (missing minHeight: 44 in the named screen).
  </action>
  <verify>
    <automated>cd frontend && node scripts/verify-touch-targets.mjs 2>&1 | tee /tmp/tt4.log; rc=${PIPESTATUS[0]}; grep -q "Button.tsx small size declares minHeight: 44 and minWidth: 44" /tmp/tt4.log && ! grep -E "FAIL: Button.tsx" /tmp/tt4.log && ! grep -E "FAIL: FinanceSourceFormScreen.tsx" /tmp/tt4.log && ! grep -E "FAIL: TransactionFormScreen.tsx missing minHeight" /tmp/tt4.log && ! grep -E "FAIL: ProfileScreen.tsx" /tmp/tt4.log && echo "==> Checks A + B green (C/E/F may still FAIL until their owning plans merge)"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-touch-targets.mjs` output contains `==>` PASS line for Check A (Button.tsx 44×44).
    - Output does NOT contain `FAIL: Button.tsx`, `FAIL: FinanceSourceFormScreen.tsx`, `FAIL: TransactionFormScreen.tsx missing minHeight`, or `FAIL: ProfileScreen.tsx`.
    - Output MAY still contain FAIL lines for Check C (TransactionFormScreen inputMode) and Checks E/F (Screen.tsx + App.tsx safe-area) — these are expected and owned by other plans.
    - `npm test` reports zero failing suites (no test regression).
  </acceptance_criteria>
  <done>MOBUI-01 verifier checks A + B green; Button.test.ts 3/3 green; no test regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a — purely client-side CSS sizing change | Touch-target sizing is a UX/a11y concern, not a security boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Tampering / data freshness | Stale-shell trap | accept | Mitigation lives in 05-03 (SW + UpdateToast). |
| T-05-04-02 | Information Disclosure | Cross-user cache leakage | accept | Out of scope. |
| T-05-04-03 | Tampering / Spoofing | Service worker hijack | accept | Mitigated by HTTPS (Phase 4). |
| T-05-04-04 | Tampering | Cache poisoning | accept | Mitigated by 05-03 Workbox config. |
| T-05-04-05 | Tampering | Reflected XSS via manifest | accept | No manifest changes in this plan. |
| T-05-04-SC | Tampering | Supply chain | accept | No npm installs in this plan. |
</threat_model>

<verification>
After all tasks in this plan complete:

```bash
cd frontend
# Button.tsx single-point fix
node -e "const s=require('fs').readFileSync('src/components/Button.tsx','utf8'); if(!/small:\s*\{[^}]*minHeight:\s*44[^}]*minWidth:\s*44/s.test(s) && !/small:\s*\{[^}]*minWidth:\s*44[^}]*minHeight:\s*44/s.test(s))process.exit(1)"
# Three screens have ≥3 minHeight:44 each
for f in src/screens/FinanceSourceFormScreen.tsx src/screens/TransactionFormScreen.tsx src/screens/ProfileScreen.tsx; do
  c=$(grep -c 'minHeight: 44' "$f"); test "$c" -ge 3 || { echo "FAIL: $f has only $c minHeight:44"; exit 1; }
done
# Button.test.ts now 3/3 GREEN
npm test -- --testPathPattern=Button.test.ts | grep -E "3 passed"
# verify-touch-targets Checks A and B green
node scripts/verify-touch-targets.mjs 2>&1 | grep -v "FAIL" | grep -E "Button.tsx small size"
```
</verification>

<success_criteria>
1. `frontend/src/components/Button.tsx` `small` size style declares both `minHeight: 44` and `minWidth: 44`.
2. `frontend/src/screens/FinanceSourceFormScreen.tsx` declares ≥3 instances of `minHeight: 44` and ≥1 `minWidth: 44`.
3. `frontend/src/screens/TransactionFormScreen.tsx` declares ≥3 instances of `minHeight: 44` and ≥1 `minWidth: 44` (in the currency picker block only — NOT on amount fields).
4. `frontend/src/screens/ProfileScreen.tsx` declares ≥3 instances of `minHeight: 44` and ≥2 `minWidth: 44` (backButton + modal close).
5. `cd frontend && npm test -- --testPathPattern=Button.test.ts` reports 3/3 passing.
6. `node frontend/scripts/verify-touch-targets.mjs` Checks A and B do NOT FAIL (other checks may still fail until their owning plans merge).
7. No regression in the existing Jest suite (`CurrencyPicker`, `TransactionsScreen`, `FinanceSourcesScreen` specs all still pass).
</success_criteria>

<output>
Create `.planning/phases/05-pwa-ify-frontend-mobile-polish/05-04-SUMMARY.md`. Record:
- The exact `git diff --stat` summary for Button.tsx + the three screen files (line counts touched).
- Confirmation that `Button.test.ts` flipped from 1/3 PASS to 3/3 PASS (paste the npm test summary line).
- Confirmation that the amount-field TextInputs in TransactionFormScreen.tsx were NOT touched (those are owned by plan 05-06).
- Exact occurrence counts of `minHeight: 44` and `minWidth: 44` per file.
</output>
