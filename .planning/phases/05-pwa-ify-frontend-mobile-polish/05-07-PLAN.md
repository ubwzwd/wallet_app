---
plan_id: 05-07
phase: 05
plan: 07
type: execute
wave: 2
depends_on: ["05-02", "05-03", "05-04", "05-05", "05-06"]
files_modified:
  - frontend/package.json
requirements:
  - PWA-01
  - PWA-02
  - PWA-03
  - PWA-04
  - MOBUI-01
  - MOBUI-02
  - MOBUI-03
  - MOBUI-04
  - MOBUI-05
autonomous: false
must_haves:
  truths:
    - "A single command (`npm run verify:phase5`) runs the full deterministic battery — `npm run build:web` + `verify-pwa.mjs --all` + `verify-touch-targets.mjs` + `npm test` — and exits 0 only when every PWA-01..04 + MOBUI-01..04 acceptance criterion is satisfied."
    - "The full Jest suite (Button.test.ts + the three pre-existing specs) passes 100%."
    - "D-12: A developer Incognito-Chrome install attempt against `https://localhost` (per VALIDATION § Manual-Only Verifications) confirms the app installs and launches in standalone mode — the spec-correct proxy for SC-2 since Lighthouse 12+ removed the PWA category (supersession of CONTEXT D-12's `--only-categories=pwa` flow recorded in 05-CONTEXT.md and RESEARCH § Pitfall 1)."
    - "MOBUI-05 (real-device install on iPhone Safari + Android Chrome) is documented as DEFERRED to Phase 6 per CONTEXT D-13, with the Phase 6 inheritance pointer recorded in the phase SUMMARY so plan-phase 06 picks it up."
  artifacts:
    - path: "frontend/package.json (scripts.verify:phase5)"
      provides: "Single npm script that chains build + static verify + tests; the canonical Phase 5 gate"
      contains: "verify:phase5"
  key_links:
    - from: "npm run verify:phase5"
      to: "npm run build:web → verify-pwa.mjs --all → verify-touch-targets.mjs → npm test"
      via: "&&-chained script"
      pattern: "verify:phase5"
  deferred:
    - id: "MOBUI-05"
      reason: "D-13: Real-device install on iPhone Safari + Android Chrome cannot be verified locally — requires a public HTTPS URL with a real Let's Encrypt cert. Per CONTEXT D-13 this is explicitly deferred to Phase 6's verification, which inherits this obligation. Phase 5 ships the Incognito-Chrome install smoke against https://localhost as the spec-correct proxy."
      inherits_to: "Phase 6 (DEPLOY-05 + DOMAIN-01..03)"
---

<objective>
Wave 2 — final phase gate. Aggregate the per-plan verifiers from 05-01..05-06 into a single `npm run verify:phase5` script, run the full battery end-to-end, document the manual Incognito-Chrome install smoke (the spec-correct SC-2 proxy since Lighthouse 12+ removed the PWA category per RESEARCH § Pitfall 1), and explicitly record MOBUI-05's deferral to Phase 6.

Purpose: A phase-level gate that downstream tooling (the orchestrator's `/gsd:verify-work` and CI integrations) can invoke as a single command. Without aggregation, each plan's verifier runs in isolation and there's no single signal that the phase as a whole has landed. The manual Incognito step is intentionally documented but NOT automated — local HTTPS + Chrome installability behavior is not reliably testable from a headless script (RESEARCH § Environment Availability flags Chromium absent in WSL).

Output: One new npm script entry (`scripts.verify:phase5`); two checkpoint:human-verify steps (one for the Incognito install smoke, one for the final phase sign-off); a SUMMARY.md that records MOBUI-05 inheritance to Phase 6.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-pwa-ify-frontend-mobile-polish/05-CONTEXT.md
@.planning/phases/05-pwa-ify-frontend-mobile-polish/05-RESEARCH.md
@.planning/phases/05-pwa-ify-frontend-mobile-polish/05-VALIDATION.md
@.planning/phases/05-pwa-ify-frontend-mobile-polish/05-01-SUMMARY.md
@.planning/phases/05-pwa-ify-frontend-mobile-polish/05-02-SUMMARY.md
@.planning/phases/05-pwa-ify-frontend-mobile-polish/05-03-SUMMARY.md
@.planning/phases/05-pwa-ify-frontend-mobile-polish/05-04-SUMMARY.md
@.planning/phases/05-pwa-ify-frontend-mobile-polish/05-05-SUMMARY.md
@.planning/phases/05-pwa-ify-frontend-mobile-polish/05-06-SUMMARY.md
@frontend/package.json
@frontend/scripts/verify-pwa.mjs
@frontend/scripts/verify-touch-targets.mjs
@infra/docker-compose.prod.yml

<interfaces>
<!-- VALIDATION.md § Sampling Rate → Phase gate chains: -->
<!--   cd frontend && npm run build:web && node scripts/verify-pwa.mjs --all && node scripts/verify-touch-targets.mjs && npm test -->
<!--   Plus Phase 4 smoke regression: bash infra/scripts/smoke.sh full (NOT in npm script — too heavy; documented in SUMMARY) -->
<!--   Plus manual Incognito install (NOT scripted; checkpoint:human-verify here) -->

<!-- CONTEXT D-13: MOBUI-05 (real-device install) is deferred to Phase 6 — must be surfaced in SUMMARY for plan-phase 06 to inherit. -->

<!-- VALIDATION.md § Manual-Only Verifications: -->
<!--   Step 1: make compose-up   (OR docker compose -f infra/docker-compose.prod.yml up -d after populating infra/.env per Phase 4) -->
<!--   Step 2: open https://localhost in an Incognito Chrome window -->
<!--   Step 3: DevTools → Application → Manifest → confirm "Installability" shows no errors -->
<!--   Step 4: address-bar install icon → click → confirm the app installs and launches in standalone mode -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `verify:phase5` npm script chaining the full deterministic battery</name>
  <read_first>
    - frontend/package.json (current scripts block — note the existing `build:web` is atomic; do NOT alter it)
    - .planning/phases/05-pwa-ify-frontend-mobile-polish/05-VALIDATION.md § Sampling Rate (the canonical phase-gate chain)
    - .planning/phases/05-pwa-ify-frontend-mobile-polish/05-CONTEXT.md (D-05 — atomicity contract for build-related scripts)
  </read_first>
  <files>frontend/package.json</files>
  <action>
    In `frontend/package.json`, add a new entry under `scripts`: `"verify:phase5": "npm run build:web && node scripts/verify-pwa.mjs --all && node scripts/verify-touch-targets.mjs && npm test"`. Place it adjacent to the existing `build:web` entry (alphabetical-ish ordering is fine; do NOT reorder existing entries).

    Do NOT modify any other scripts entry. Do NOT modify dependencies or devDependencies (those were settled in 05-01). Do NOT split the new script across multiple lines — it MUST be a single &&-chained line so it fails fast on the first error.
  </action>
  <verify>
    <automated>cd frontend && node -e "const p=require('./package.json'); const s=p.scripts['verify:phase5']; if(!s)throw new Error('verify:phase5 missing'); if(!s.includes('npm run build:web'))throw new Error('build:web step missing'); if(!s.includes('verify-pwa.mjs --all'))throw new Error('verify-pwa step missing'); if(!s.includes('verify-touch-targets.mjs'))throw new Error('verify-touch-targets step missing'); if(!s.includes('npm test'))throw new Error('npm test step missing'); console.log('==> verify:phase5 script registered');"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/package.json` `scripts.verify:phase5` contains, in order: `npm run build:web`, `verify-pwa.mjs --all`, `verify-touch-targets.mjs`, `npm test` (chained with `&&`).
    - The value is a single string (no embedded newlines).
    - No other scripts entry was modified.
  </acceptance_criteria>
  <done>Single-command phase-gate script registered.</done>
</task>

<task type="auto">
  <name>Task 2: Run npm run verify:phase5 end-to-end and confirm full green</name>
  <read_first>
    - frontend/package.json (the verify:phase5 entry from Task 1)
    - .planning/phases/05-pwa-ify-frontend-mobile-polish/05-*-SUMMARY.md (every prior plan's SUMMARY — pre-existing context)
  </read_first>
  <files>(no edits; verification gate)</files>
  <action>
    From `frontend/`, run `npm run verify:phase5`. The command MUST exit 0. The pipeline runs in this order:

    1. `rm -rf dist` (build:web step)
    2. config swap to prod
    3. `expo export -p web`
    4. `npx workbox generateSW workbox-config.cjs`
    5. config swap back to dev
    6. `node scripts/verify-pwa.mjs --all` (manifest + apple-meta + viewport + sw + ios-zoom — all five PWA static checks)
    7. `node scripts/verify-touch-targets.mjs` (Checks A through F — touch targets + safe-area + input modes + email props)
    8. `npm test` (Button.test.ts + CurrencyPicker.test.ts + TransactionsScreen.test.ts + FinanceSourcesScreen.test.ts — all 4 specs)

    If any step fails, the chain stops and the verifier exits non-zero. Inspect the failure and route back to the owning plan (manifest fail → 05-02; sw fail → 05-03; touch-target fail → 05-04 or 05-05 or 05-06; test fail → the related Wave-1 plan).

    Confirm the final exit code is 0 and that the output includes the `==>` cadence from all three verifier scripts plus the Jest "X passed" summary.

    Optionally (recommended but not gating), also run Phase 4's smoke against the new build to confirm no regression: from the repo root, run `bash infra/scripts/smoke.sh full` (requires `infra/.env` per Phase 4). If the smoke fails on a Phase-5-related reason (e.g. a serving issue with `manifest.json` MIME type), document and fix; if it fails on a Phase-4 reason unrelated to PWA assets, that's an inherited Phase-4 issue and not a Phase-5 blocker.
  </action>
  <verify>
    <automated>cd frontend && npm run verify:phase5 2>&1 | tee /tmp/phase5-gate.log; rc=${PIPESTATUS[0]}; test "$rc" = "0" && grep -E "==> .* manifest" /tmp/phase5-gate.log && grep -E "==> .* (touch-targets|Button.tsx)" /tmp/phase5-gate.log && grep -E "(Tests:.*passed|Test Suites:.*passed)" /tmp/phase5-gate.log && echo "==> verify:phase5 GREEN end-to-end"</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run verify:phase5` exits 0.
    - The output shows the `==> manifest:` / `==> apple-meta:` / `==> viewport:` / `==> sw:` / `==> ios-zoom:` cadence from verify-pwa.mjs.
    - The output shows the `==> touch-targets:` cadence from verify-touch-targets.mjs (Button + 3 screens + safe-area-context import + SafeAreaProvider + inputMode parity + email props all GREEN).
    - The output shows the Jest summary line indicating all 4 test suites passed (Button.test.ts now 3/3 green).
  </acceptance_criteria>
  <done>Full deterministic Phase 5 battery exits 0 in a single command.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The phase-gate battery has just exited 0 (Task 2). Every PWA-01..04 + MOBUI-01..04 acceptance criterion is satisfied by the static verifiers. What is NOT covered by automation is the actual install behavior of the PWA in a real browser — per VALIDATION § Manual-Only Verifications, the spec-correct proxy for SC-2 (post Lighthouse-12 PWA-category removal) is a developer Incognito-Chrome install attempt against the local prod stack.</what-built>
  <how-to-verify>
    1. Ensure `infra/.env` is populated per Phase 4 (CADDY_DOMAIN, CADDY_TLS_MODE, SECRET_KEY, POSTGRES_PASSWORD, ALLOWED_ORIGINS — see `infra/.env.example`).
    2. From the repo root, start the production compose stack: `docker compose -f infra/docker-compose.prod.yml up -d`. Wait ~30 seconds for migrations + API ready.
    3. Open Google Chrome (or Chromium) in a fresh Incognito window. Navigate to `https://localhost`. You'll see a self-signed-cert warning; click "Advanced" → "Proceed to localhost (unsafe)" (the warning is expected per Phase 4 D-10 `tls internal`).
    4. Open Chrome DevTools (F12) → Application tab → Manifest. Confirm: manifest is parsed without errors; "Installability" section shows NO errors; theme_color shows `#0ea5e9`; the 3 icons render correctly in the icon preview.
    5. In DevTools → Application → Service Workers, confirm: a service worker is registered at scope `/`, status "activated and is running", source `sw.js`.
    6. In the Chrome address bar, look for the install icon (computer-with-down-arrow). Click it. The browser prompts "Install Wallet?". Click "Install".
    7. Confirm the app opens in a standalone window (no Chrome chrome — no address bar, no tabs). The Wallet icon appears on the OS dock/taskbar.
    8. Close the standalone window. Tear down the compose stack: `docker compose -f infra/docker-compose.prod.yml down`.

    If any step fails: file the specific symptom (e.g. "Installability section shows: No matching service worker detected. You may need to reload the page, or check that the scope of the service worker for the current page encloses the scope and start URL from the manifest."). Common fix targets: (a) SW scope mismatch → check `dist/sw.js` is served from `/sw.js` not a subpath; (b) manifest parse error → re-run `node frontend/scripts/verify-pwa.mjs --check=manifest`; (c) the install icon doesn't appear → Chrome's installability heuristic may require ≥30s engagement; reload the page and wait.
  </how-to-verify>
  <resume-signal>Type "installed" (with optional notes about the install experience) once the Incognito install completes and the standalone window launches. Type "blocked: <reason>" if any step fails — that creates a gap-closure follow-up.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Phase 5 — PWA-ify Frontend + Mobile Polish — has shipped:

  - Wave 0 (05-01): verify-pwa.mjs (ajv + vendored W3C schema) + verify-touch-targets.mjs + Button.test.ts; workbox-cli + ajv installed as devDependencies (no human-verify checkpoint needed — both are widely-deployed standards).
  - Wave 1 (05-02 through 05-06): full public/ directory authored (manifest, pwa.css, offline.html, 4 PNG icons, favicon.ico, custom index.html); workbox-config.cjs generated SW with NetworkFirst + skipWaiting + clientsClaim; UpdateToast wired into App.tsx; SafeAreaProvider wraps the root tree; Screen.tsx imports SafeAreaView from safe-area-context; Button.tsx single-point 44×44 fix; non-Button spot-fixes in 3 screens; inputMode="decimal" on amount fields.
  - Wave 2 (05-07): single-command `npm run verify:phase5` exits 0; manual Incognito-install smoke confirmed (previous task).

  9/9 phase requirements satisfied:
  - PWA-01 (manifest) — static
  - PWA-02 (iOS meta) — static
  - PWA-03 (viewport-fit=cover) — static
  - PWA-04 (Workbox SW with NetworkFirst + skipWaiting + clientsClaim + update toast) — static + manual install confirms registration
  - MOBUI-01 (44×44 touch targets) — static + Jest test
  - MOBUI-02 (safe-area-inset wiring) — static
  - MOBUI-03 (16px input CSS) — static + visible at install time
  - MOBUI-04 (inputMode=decimal + email autocomplete) — static
  - MOBUI-05 (real-device install on iPhone Safari + Android Chrome) — DEFERRED to Phase 6 per CONTEXT D-13.</what-built>
  <how-to-verify>
    1. Re-run `cd frontend && npm run verify:phase5` once more — must still exit 0.
    2. Confirm that `.planning/phases/05-pwa-ify-frontend-mobile-polish/` contains all six SUMMARY.md files (05-01 through 05-06) AND that none of them flagged a regression that wasn't addressed.
    3. Confirm `.planning/STATE.md` will be updated post-phase to reflect Phase 5 complete (the orchestrator handles this).
    4. Confirm the deferral pointer for MOBUI-05 → Phase 6 is recorded in this plan's must_haves.deferred frontmatter and will be surfaced in the SUMMARY (Task 5 below).
  </how-to-verify>
  <resume-signal>Type "approved" to sign off Phase 5 and unblock `/gsd:verify-work`. Type "rejected: <reason>" if any gap remains — that triggers a gap-closure plan via `/gsd:plan-phase 05 --gaps`.</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Write phase SUMMARY recording deferred MOBUI-05 inheritance to Phase 6</name>
  <read_first>
    - .planning/phases/05-pwa-ify-frontend-mobile-polish/05-*-SUMMARY.md (per-plan SUMMARYs — aggregate the key facts)
    - .planning/phases/05-pwa-ify-frontend-mobile-polish/05-CONTEXT.md (D-13 deferral text)
    - .planning/ROADMAP.md (Phase 6 entry — confirm the deferral will be visible to plan-phase 06)
  </read_first>
  <files>.planning/phases/05-pwa-ify-frontend-mobile-polish/05-07-SUMMARY.md</files>
  <action>
    Create `.planning/phases/05-pwa-ify-frontend-mobile-polish/05-07-SUMMARY.md`. The SUMMARY MUST contain:

    1. A header recording phase + plan + completion date.
    2. A bullet list of all 8 requirement IDs satisfied (PWA-01..04, MOBUI-01..04) with the exact verifier/script that confirms each.
    3. An explicit "Deferred to Phase 6" section naming MOBUI-05 (real-device install on iPhone Safari + Android Chrome) and quoting the rationale from CONTEXT D-13 verbatim. The section must include the inheritance pointer for plan-phase 06 (`Phase 6 verify gate MUST cover MOBUI-05 against the real https://<domain> with a real Let's Encrypt cert`).
    4. The final `npm run verify:phase5` output's summary line (Tests:, Test Suites:).
    5. The result of the Incognito-Chrome install smoke from Task 3 (paste the resume-signal: "installed" + any notes).
    6. A note about the local Lighthouse-CI: NOT run for SC-1 because Lighthouse 12+ removed the PWA category per RESEARCH § Pitfall 1; the decomposed verify-pwa.mjs checks are the spec-correct replacement.
    7. Files touched across the phase (aggregate of files_modified from all 7 plans).
  </action>
  <verify>
    <automated>test -f .planning/phases/05-pwa-ify-frontend-mobile-polish/05-07-SUMMARY.md && grep -F "MOBUI-05" .planning/phases/05-pwa-ify-frontend-mobile-polish/05-07-SUMMARY.md && grep -F "Phase 6" .planning/phases/05-pwa-ify-frontend-mobile-polish/05-07-SUMMARY.md && grep -F "deferred" .planning/phases/05-pwa-ify-frontend-mobile-polish/05-07-SUMMARY.md && grep -F "verify:phase5" .planning/phases/05-pwa-ify-frontend-mobile-polish/05-07-SUMMARY.md && echo "==> 05-07-SUMMARY.md records MOBUI-05 inheritance"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/05-pwa-ify-frontend-mobile-polish/05-07-SUMMARY.md` exists.
    - File contains the literal substrings `MOBUI-05`, `Phase 6`, `deferred`, `verify:phase5`.
    - File aggregates the per-plan SUMMARYs into a phase-level result.
    - D-12: Supersession recorded — the SUMMARY names the Lighthouse PWA-category removal (Lighthouse 12.0, 2024-05) as the reason CONTEXT D-12's `--only-categories=pwa` flow is superseded; the decomposed verify-pwa.mjs static checks + manual Incognito install smoke are the spec-correct replacement per RESEARCH § Pitfall 1.
  </acceptance_criteria>
  <done>Phase SUMMARY committed; MOBUI-05 inheritance recorded for Phase 6; D-12 supersession surfaced from PLAN to SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Aggregator script ↔ subscripts | `npm run verify:phase5` chains existing trusted scripts; no new external trust |
| Manual Incognito install ↔ local prod stack | Self-signed cert (Phase 4 `tls internal`) means trust is established via "Proceed to localhost (unsafe)" click — acceptable for local verification per CONTEXT D-12 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07-01 | Tampering / data freshness | Stale-shell trap | mitigate | The Task 3 Incognito smoke explicitly confirms the SW is registered + activated + controlling the page, validating the 05-03 mitigation works in the real browser. |
| T-05-07-02 | Information Disclosure | Cross-user cache leakage | accept | Out of scope; deferred per RESEARCH § Security Domain. |
| T-05-07-03 | Tampering / Spoofing | Service worker hijack | mitigate | HTTPS verified during Task 3 (Caddy `tls internal` on localhost; real Let's Encrypt on Phase 6 deployment). |
| T-05-07-04 | Tampering | Cache poisoning | mitigate | Confirmed during install smoke (Application → Cache Storage shows precache bucket with content-hashed URLs; cleanupOutdatedCaches active). |
| T-05-07-05 | Tampering | Reflected XSS via manifest | accept | Static manifest authored by us. |
| T-05-07-SC | Tampering | Supply chain (npm) | accept | No new npm installs in this plan. |
</threat_model>

<verification>
After all tasks in this plan complete:

```bash
# Single-command full battery
cd frontend && npm run verify:phase5
# Per-plan SUMMARYs all exist
ls .planning/phases/05-pwa-ify-frontend-mobile-polish/05-0{1,2,3,4,5,6,7}-SUMMARY.md
# MOBUI-05 inheritance recorded for Phase 6
grep -F "MOBUI-05" .planning/phases/05-pwa-ify-frontend-mobile-polish/05-07-SUMMARY.md
grep -F "Phase 6" .planning/phases/05-pwa-ify-frontend-mobile-polish/05-07-SUMMARY.md
```

Plus the two human-verify resume-signals: "installed" (Task 3) and "approved" (Task 4).
</verification>

<success_criteria>
1. `cd frontend && npm run verify:phase5` exits 0 — the full deterministic battery (build + verify-pwa --all + verify-touch-targets + Jest) green end-to-end.
2. Manual Incognito-Chrome install smoke against `https://localhost` confirms the app installs and launches in standalone mode (Task 3 resume-signal: "installed").
3. `05-07-SUMMARY.md` records MOBUI-05 deferral to Phase 6 with the verbatim rationale from CONTEXT D-13.
4. The phase sign-off checkpoint (Task 4) approved by the developer — green-lights `/gsd:verify-work` for Phase 5.
5. All 8 statically-verifiable requirements (PWA-01..04, MOBUI-01..04) are confirmed satisfied by the per-plan verifiers; MOBUI-05 is explicitly inherited by Phase 6.
6. No regression in Phase 4's smoke (recommended check; not gating).
</success_criteria>

<output>
Create `.planning/phases/05-pwa-ify-frontend-mobile-polish/05-07-SUMMARY.md` (Task 5 already produces this). This is also the phase-level result document — it MUST aggregate cross-plan facts and surface the MOBUI-05 → Phase 6 inheritance so `/gsd:plan-phase 06` picks it up.
</output>
